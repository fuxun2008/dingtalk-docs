---
title: "Triggers and actions overview"
description: "To make automations match your needs, configure triggers and actions sensibly. This page walks through both."
---

## 1 What it does

To make automations match your needs, configure triggers and actions sensibly. This page walks through both.

## 2 How to use

Create a new automation — the left panel hosts the trigger and action config.

### 2.1 Triggers

"On record create", "On record change", "On new/modified record meets condition", "On record time", "On button click", "On form submit", "Scheduled", and "On data received".

![](/aitable/automation/triggers-and-actions/images/image_LyHFoMSQ-g.png)

### 2.2 100+ action nodes covering every scenario

We're working hard to respond to user requests — bridging in-product scenarios and onboarding new ones aggressively:

- Smoother integration with DingTalk's other products: start OA approvals, create / update Teambition tasks, create & generate docs, send corp email, etc.
- Richer AI: call the AI assistant, knowledge learning & retrieval, creative content generation; also Coze, Bailian, and other platform workflows.
- More third-party scenarios: Amap, Wanwu Yiyuan, 1688, Enterprise Wikipedia, etc.

![](https://alidocs.dingtalk.com/core/api/resources/img/5eecdaf48460cde55ffd2766da321891b20b14ca677bd45875b8339e1c4c24834322b4c2cdd63b4d6b58639871ed7e1ba156a98577f418d5835def8d184879c62333b774308bca2db56cb8da074bce1af4942ce49acd239aad7998aaedd7a62e?tmpCode=28e6aab5-0615-48cf-9893-260e09bb6633)

![](https://alidocs.dingtalk.com/core/api/resources/img/5eecdaf48460cde55ffd2766da321891b20b14ca677bd45875b8339e1c4c24834322b4c2cdd63b4d6b58639871ed7e1ba156a98577f418d51feca8d9a274dec897d24849d941749548681ce2be076270409ea7a5fc73d03b071637786261327a?tmpCode=28e6aab5-0615-48cf-9893-260e09bb6633)

## 3 Details

### 3.1 Triggers

#### 3.1.1 On record create

Fires when a new record is added to a table.

You can add **simultaneously-met conditions** to constrain the trigger further.

**Note**: clicking **New** creates a blank row — every field is empty. If a downstream action references any non-system field (i.e. anything other than Created by / Last modified by / Created time / Last modified time) from the just-created record, the value is actually empty and the action will error out.

Use case: when a new record is added, ping the owner.

![](/aitable/automation/triggers-and-actions/images/image_r5DOGnipN2.png)

#### 3.1.2 On record change

Fires when the specified field changes.

You can add **simultaneously-met conditions** to constrain it further.

Fields not supported as the watched field: Button, Auto number, Created by, Created time, and any field expanded from a Person field.

Use case: when **Task progress** flips to "Done", auto-notify the owner.

![](/aitable/automation/triggers-and-actions/images/image_fxLHE-IYWC.png)

#### 3.1.3 On new/modified record meets condition

When a newly added or modified record matches the condition, downstream fires. Existing records that already match will not fire.

Any field type except Button is allowed in the condition.

**Note**: only changes to the fields you've set, that meet the condition, fire — changes to other fields don't. This is the difference between "On new/modified record meets condition" and "On record change". To watch any field's change while requiring a value on a specific field, use **On record change**.

![](/aitable/automation/triggers-and-actions/images/image_5IWM15ruqS.png)

#### 3.1.4 On record time

Fires when the time set in a record arrives.

You can add **simultaneously-met conditions** to constrain it further.

This trigger is one-shot — no repeat / recurrence. Only **Date** and **Created time** fields can be the trigger field.

Use case: project management at fixed time milestones — ping the team.

![](/aitable/automation/triggers-and-actions/images/image_EwpI1C08Mv.png)

#### 3.1.5 On button click

Fires when a button is clicked in the AI Table.

"Button" includes both the Button field on a record and the bottom-of-card buttons on a message card.

Use case: human-in-the-loop — after a manager approves, send the approval notice.

![](/aitable/automation/triggers-and-actions/images/image_9iVuzbCKp8.png)

![](/aitable/automation/triggers-and-actions/images/image_avT5gfctmi.png)

#### 3.1.6 On form submit

Fires when a new form record is submitted in AI Table.

You can add **simultaneously-met conditions** to constrain it further.

Difference from **On record create**: the way the new record is added differs — and form submit avoids the empty-value pitfall.

![](/aitable/automation/triggers-and-actions/images/image_PuLvdP5BKo.png)

#### 3.1.7 Scheduled

Fires at a specific time.

Custom date / time and optional repeat.

**Note**: if you set "the 31st of every month, monthly repeat", a month without a 31st will skip. Same for February — a date that doesn't exist in February skips that month.

Use case: anything periodic / repeating, e.g. weekly project-progress check.

![](/aitable/automation/triggers-and-actions/images/image_flhHDSb4zO.png)

#### 3.1.8 On data received

Set up a webhook to receive data from internal or external systems in real time — sync data across AI Tables and from external systems into AI Table.

When the upstream system changes, it sends an HTTP POST to the AI Table automation. As soon as the request lands, the automation fires.

See: [Webhook] On-data-received setup guide.

![](/aitable/automation/triggers-and-actions/images/image_VWtpJZ8bgj.png)

### 3.2 Actions

#### 3.2.1 Send DingTalk message

Custom message body, sent via a bot to a specific user or group.

Send as **User account** or **Doc automation assistant**.

The message arrives as a card — customize the title and body, reference AI Table fields, attach images, and add buttons.

![](/aitable/automation/triggers-and-actions/images/image_dRTWeKIFMR.png)

#### 3.2.2 Create record

Add a row to a table. Use **Set field value** to fill each field.

Field types **not** settable: Auto number, Lookup, Button, One-way link, Two-way link, Linked reference, Created by, Last modified by, Created time, Last modified time, computed Formula, and any field expanded from a Person field.

Also: Attachment, Barcode, and Geolocation fields aren't settable when configured as "mobile only".

![](/aitable/automation/triggers-and-actions/images/image_TWzY4SpUjr.png)

#### 3.2.3 Update record

Modify specific records in a table.

- Update all records, or filter to update only matching ones.
- If **Update record** uses the same table as the prior step, you can pick the prior step's record to update.
- Update multiple fields in one record — click **Set field value** to set each.

Field types **not** settable: Auto number, Lookup, Button, One-way link, Two-way link, Linked reference, Created by, Last modified by, Created time, Last modified time, computed Formula, and any field expanded from a Person field.

Also: Attachment, Barcode, and Geolocation fields aren't settable when configured as "mobile only".

![](/aitable/automation/triggers-and-actions/images/image_z88-N74K-p.png)

#### 3.2.4 Look up records

Look up records by condition; the result feeds downstream.

- **Set the filter** — pick all records matching the condition.
- **Pick what to look up** — the fields you want available downstream.

Note: **Look up records** is an intermediate step, never the final step of an automation.

When a Lookup field is the filter target, only equals / contains / is empty work — for text or number value searches. If the look-up depends on a checkbox state (✓), you may not match. Switch the condition to a searchable text or number, or use a formula to convert the checkbox state to "true" / "false" text.

![](/aitable/automation/triggers-and-actions/images/image_VNEH3iLome.png)

#### 3.2.5 Conditional branch

A conditional branch (if-else) is an action AI Table automation can run — based on whether the condition is met, it routes downstream. Set what to do on match and on no-match — and you've built multiple branches.

Common scenarios:

- CRM: route by customer info or opportunity stage. E.g. when company size = "Large", record into the "Large company" table; otherwise into the "SMB" table.
- Inventory: route by stock state. E.g. when an employee files a goods request, condition = "stock ≥ requested" — if met, approve and decrement stock; if not, notify the requester and DingTalk-message the inventory manager to restock.
- Resume-bank dedup: when a candidate enters the interview-stage table, auto-check the resume bank — if absent, auto-add to the bank.

![](/aitable/automation/triggers-and-actions/images/image_wydbBL8XfC.png)

#### 3.2.6 Loop

AI Table automation supports loops. Set rules and it repeats the same operation until done.

Common scenarios:

- Targeted to-do dispatch: every week, look up tasks with status "In progress" and DM each owner with only their own tasks.
- Per-interviewer record isolation: for all completed interviews today, auto-create a new record for each interviewer to capture their result, and DM them to fill it. They click the card button to fill straight in the AI Table — they don't see other interviewers' results, and don't get notifications about other interviewers.

![](/aitable/automation/triggers-and-actions/images/image_7xWaFf_GyS.png)

#### 3.2.7 HTTP request

Hit a URL — call third-party platforms to process data.

**Note**: this feature is more advanced and may need some programming background plus AI Table experience to fully exploit.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-2_hxb0sQhLGp.png)

#### 3.2.8 AI capabilities

1. **Ask AI**

Have AI generate text per your spec, or answer your question.

- Reference AI Table data or earlier-step results to polish or summarize, and emit a text block.
- The text feeds the current step and downstream automation steps.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-5_-0zcXX1lwf.png)

2. **Parameter extraction**

Extract specified fields from a block of text — turn unstructured into structured.

3. **Image understanding**

AI answers a question about an input image.

4. **Personal-ID OCR**

AI extracts info from attachments — e.g. ID cards.

#### 3.2.9 To-do

Create a to-do for a specified person.

In the automation you can set the title, owner, due date, participants, and description.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x_qxk3z4KTUO.png)

By passing the **task ID** you can complete, restart, or delete a previously created to-do.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-14_q24TSRODBy.png)

The task ID is a variable generated when **Create to-do** runs — get it via the step variable of **Create DingTalk to-do**.

**Tip:**

- Add a Text field named "Task ID" to your AI Table.
- After **Create to-do**, add an **Update record** step — store the generated task ID in the AI Table for later use.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-16_finQuEeaxg.png)

#### 3.2.10 Group chat

Based on AI Table content, create a DingTalk group chat directly in the automation.

Set the group name, type, members, and owner — up to 50 members.

The created group can be written into a Group field, or used as the recipient of DingTalk messages.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-17_gE9P2uWOCe.png)

#### 3.2.11 Calendar event

Based on AI Table content, create a calendar event in the automation — set the title, start / end time, and participants directly.

**Note**: events created via automation default to a 5-minute reminder; the automation panel doesn't expose this — you have to adjust it inside the calendar event detail.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-2_nKwhhdEt9e.png)

By passing the **event ID** you can delete a previously created DingTalk calendar event. Get the ID from the **Create DingTalk calendar event** step variable. Tip:

- Add a Text field named "Event ID" to your AI Table.
- After **Create event**, add an **Update record** step — store the generated event ID in the AI Table for later use.

![](/aitable/automation/triggers-and-actions/images/局部截图@1x-3_uoexsMKvOB.png)

#### 3.2.12 Other

Other business-app integrations are not enumerated here — for usage, contact the connector platform.
