---
title: "ドキュメントのデータ構造"
description: "オンラインドキュメントは複数のブロック要素から構成されるツリーです。BlockElement のタイプによって内部にネストできる要素のタイプが異なります。例えば、ハイライトブロックには任意の BlockElement をネストできますが、段落ブロックには InlineElement しかネストできません。ドキュメントの構造はおおよそ以下のようになります。"
---

オンラインドキュメントは複数のブロック要素から構成されるツリーです。`BlockElement` のタイプによって内部にネストできる要素のタイプが異なります。例えば、`ハイライトブロック` には任意の `BlockElement` をネストできますが、`段落ブロック` には `InlineElement` しかネストできません。ドキュメントの構造はおおよそ以下のようになります。

このドキュメントを例として、ページを分解すると以下のようになります。
