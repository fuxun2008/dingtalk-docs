---
title: "IF"
description: "IF関数の使い方を解説。Yidaのフォームやワークフロー設計で条件判定を行い、論理式の真偽に応じて異なる値を返す方法や、対応シナリオ、書式、パラメータ、活用例を紹介します。"
polished: true
---
**IF** 関数は条件判定に使用します。条件を満たす場合は指定した値を返し、満たさない場合は別の値を返します。

## 対応する製品シナリオ

IF 関数は、以下の Yida シナリオで対応しています。

- フォーム（メインテーブルのフィールド）
- フォーム（業務関連ルール）
- フォーム（検証）
- ワークフロー設計（検証ルール）
- ワークフロー設計（関連アクション）
- 統合と自動化
- 作業フローノード
- コネクタ

## 書式

```text
IF(condition,value1,value2)
```

## 属性の説明

| **属性** | **必須** | **説明** |
|---|---|---|
| condition | はい | 判定に使用する論理式です。 |
| value1 | はい | 論理式が **true** と評価された場合に返す値です。 |
| value2 | はい | 論理式が **false** と評価された場合に返す値です。 |

## 戻り値

戻り値は、`IF()` 関数の value1 または value2 のいずれかです。

- 論理式 condition が true と評価された場合、value1 を返します。
- 論理式 condition が false と評価された場合、value2 を返します。

## 例

60 点未満の場合は「不合格」、60 点以上の場合は「合格」を返します。

国語・数学・英語の点数がすべて 60 点以上の場合は「適格」、いずれか 1 科目でも 60 点未満の場合は「不適格」を返します。