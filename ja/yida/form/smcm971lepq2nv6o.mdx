---
title: "MOD"
description: "YidaのMOD関数の使い方を解説します。ある数値を別の数値で割った余りを算出し、フォーム・ワークフロー設計・統合と自動化などさまざまなシーンで活用できる数値計算関数の構文と実例を紹介します。"
polished: true
---

**MOD** 関数は、ある数値を別の数値で割った余りを返します。

## 対応シーン

MOD 関数は、以下の Yida シーンに対応しています。

- フォーム（メインテーブルフィールド）
- フォーム（業務関連ルール）
- フォーム（検証）
- ワークフロー設計（検証ルール）
- ワークフロー設計（関連操作）
- 統合と自動化
- 作業フローノード
- コネクタ

## 構文

```text
MOD(number,divisor)
```

## 属性

| 属性 | 必須 | 説明 |
|---|---|---|
| number | はい | 数値タイプ。被除数。 |
| divisor | いいえ | 数値タイプ。除数。 |

## 戻り値

| 戻り値タイプ | 説明 |
|---|---|
| 数値 | 被除数を除数で割った余り。 |

## 例

`MOD()` を使用して、`17` を `5` で割った余りを計算します。

## 結果

2 つの数値コンポーネントの値を割った余りを返します。