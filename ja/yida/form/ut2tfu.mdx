---
title: "数値"
description: "YiDA の数値コンポーネントの使い方を解説します。単位・小数点桁数・桁区切りの設定方法から、金額・年齢・数量の入力、集計や統計分析への活用手順まで、フォーム設計者向けに詳しくご紹介します。"
polished: true
---

<Warning>

重要:

- 数値コンポーネントは、計算に使用する値にのみご利用ください。銀行カード番号、身分証番号、電話番号などの値には、一行テキストコンポーネントをご使用ください。
</Warning>

## 1. ユースケース

数値コンポーネントは、金額・年齢・数量などの数値情報を入力する際に使用します。このコンポーネントで入力された数値は、計算、統計分析、集計に利用できます。

[コンポーネントの基本](/ja/yida/form/snftao)

<Note>
コンポーネントの属性、使い方、例については、[**こちら**](/ja/open/yida/components/interface)をご参照ください。
</Note>

## 2. 基本機能

### 2.1 単位

数値には「CNY」「冊」「ページ」などの単位を設定できます。単位は必要に応じてカスタマイズしてください。以下の例をご覧ください:

### 2.2 小数点桁数

保持する小数点桁数を設定できます。正確な小数値を含む金額を入力する際に便利です。以下の例をご覧ください:

小数点桁数を設定します。

### 2.3 桁区切り

大きな数値を読みやすくするため、数値コンポーネントの右側の属性パネルに**桁区切り**トグルを用意しています。

右側パネルのトグル:

桁区切り。

## 3. FAQ

<AccordionGroup>
<Accordion title="3.1 数値コンポーネントで 16 桁を超える数値を入力できないのはなぜですか？">
  数値コンポーネントの最大値は `9007199254740991`、最小値は `-9007199254740991` です。この範囲を超える値には、[**一行テキスト**](/ja/yida/form/fsuzg9)コンポーネントをご使用ください。
</Accordion>
</AccordionGroup>

### 3.2 小数点桁数の設定が反映されない

現時点では、小数点桁数の設定は数値コンポーネントに手動で入力された値にのみ適用されます。値が**数式計算**によって生成される場合は、`ROUND` などの[数学関数](/ja/yida/form/nn2gzi)を使用して小数点桁数を制御してください。

<AccordionGroup>
<Accordion title="3.3 デフォルトに小数を入力できないのはなぜですか？">
  数値コンポーネントのデフォルトは、小数を直接サポートしていません。代わりに、以下のように変数バインディングをご利用ください:

</Accordion>
<Accordion title="3.4 入力された数値を自動的にパーセンテージ形式にするにはどうすればよいですか？">
  テキストコンポーネントをフォームにドラッグし、以下の数式を設定してください。

</Accordion>
<Accordion title="3.5 数値コンポーネントの小数点桁数が多い場合に、計算が機能しないのはなぜですか？">
  数値コンポーネントの単位が % になっていないかご確認ください。% の場合、値を数式計算に反映させるため、少なくとも 4 桁の小数（1% 未満の値の場合）を入力してください。
</Accordion>
</AccordionGroup>