---
title: "一括インポート"
description: "YiDAフォームでのデータ一括インポート方法を解説します。対応コンポーネント、Excelフォーマット要件、業務ルールや通知連携の設定を含め、フォーム管理者が効率的にデータ移行を行う手順を紹介します。"
polished: true
---

| **機能** | **Freeプラン** | **ベーシック版** | **プロフェッショナル版** | **専用版** |
|---|---|---|---|---|
| **フォームデータの一括編集で検証、関連、メッセージ通知に対応** | 非対応 | 対応 | 対応 | 対応 |
| **一括インポートしたフォームデータの一括削除** | 非対応 | 対応 | 対応 | 対応 |
| **一括インポートしたフォームデータでの検証ルール適用** | 非対応 | 対応 | 対応 | 対応 |
| **通常フォームのデータインポート上限** | 5,000件、ファイルサイズ上限 **10 MB** | 5,000件、ファイルサイズ上限 **10 MB** | 5,000件、ファイルサイズ上限 **10 MB** | 50,000件、ファイルサイズ上限 **10 MB** |
| **ワークフローフォームのデータインポート上限** | 1,000件、ファイルサイズ上限 **20 MB** | 1,000件、ファイルサイズ上限 **20 MB** | 1,000件、ファイルサイズ上限 **20 MB** | 10,000件、ファイルサイズ上限 **20 MB** |

YiDAフォームはデータの一括インポートに対応しており、フォーム管理者はデータを迅速に移行できます。一括インポート時には、フォームに設定済みの検証ルール、業務ルール、統合と自動化フロー、メッセージ通知、サードパーティコールバックを合わせてトリガーすることも可能です。本記事では、フォームデータを一括インポートする方法を説明します。

- 対応コンポーネント：一行テキスト、複数行テキスト、単一選択、ドロップダウン単一選択、複数選択、ドロップダウン複数選択、画像アップロード、添付、ユーザー、部門、数値、日付、日付範囲、サブフォーム、関連フォーム（メインテーブル）。
- 非対応コンポーネント：画像テキスト表示、リッチテキスト、カスケード選択、国/地域、位置情報、署名、関連フォーム（サブテーブル）。
- 一括インポートしたデータは、Excelファイルの行順で表示されます。

## 手順

以下の手順でデータを一括インポートします。

1. YiDAワークベンチにログインし、アプリのページ管理画面を開きます。
2. データをインポートしたいフォームを選択し、**データ管理** > **インポート** をクリックします。

<Frame>
  ![](https://gw.alicdn.com/imgextra/i2/O1CN01Kugx5vY3YDF2L8qj_!!6000000003223-2-tps-1153-428.png)
</Frame>

3. インポートルールを設定します。

| インポートルールのオプション | 説明 |
|---|---|
| 高速インポート | 高速インポートを有効にすると、システムはExcelデータをバッチで取り込むため、インポート時間を大幅に短縮できます。シンプルなインポートシナリオ（レコード間に依存関係がない場合）で利用してください。<br />複雑なシナリオ（インポートデータに上流または下流の処理ロジックが必要な場合など）では有効化しないでください。 |
| インポート方式 | 3つのモードがあります：レコードの新規追加のみ、レコードの更新のみ、レコードの新規追加または更新。<br />**注意**：ワークフローフォームはレコードの新規追加のみに対応しています。 |
| インポートルール | 一括インポート時に以下のオプションを選択できます：<br />• **ワークフローフォーム**：業務通知を送信し、ワークフローの参加者がメッセージ通知を受信できるようにします。<br />• **通常フォーム**：検証ルール、業務ルール、統合と自動化フロー、メッセージ通知、サードパーティコールバックをトリガーします。 |

4. フォームデータのテンプレートをダウンロードし、サンプルに沿ってデータを編集します。

<Frame>
  ![](https://gw.alicdn.com/imgextra/i2/O1CN01e2VMp7bsOKB7HPuq_!!6000000000190-2-tps-3586-1966.png)
</Frame>

<Note>
スムーズにインポートするため、まずテンプレートをダウンロードし、提供されたサンプルに従ってください。
</Note>

<Frame>
  ![](https://gw.alicdn.com/imgextra/i2/O1CN01uOlMItJ0ygB2L8yE_!!6000000006272-2-tps-1153-893.png)
</Frame>

5. 一括インポート対象のファイルをアップロードします。`.xls` および `.xlsx` 形式に対応しています。
6. アップロード完了後、データプレビューを確認し、フォームの **フィールド** マッピングと **提出者** を設定して、**インポート** をクリックしインポート処理を開始します。

7. インポートが完了したら、**閉じる** をクリックして終了します。

<Frame>
  ![](https://gw.alicdn.com/imgextra/i1/O1CN01SE72xH9hPmH2L8vD_!!6000000004666-2-tps-1153-706.png)
</Frame>

## Excelフォーマット要件

一括インポートは、各コンポーネントで求められるフォーマットに厳密に従う必要があります。フォーマットが正しくないデータはインポートに失敗します。

各YiDAコンポーネントに対するExcelフォーマット要件は以下のとおりです。

| **コンポーネント** | **フォーマット** | **例** |
|---|---|---|
| **一行テキスト**<br />**複数行テキスト** | スプレッドシート内でテキスト形式のコンテンツ。 | これは一行テキストのサンプルです |
| **数値** | スプレッドシート内で数値形式のコンテンツ。 | 1234 |
| **単一選択**<br />**ドロップダウン単一選択** | テキスト形式。Excelファイル内で選択肢が100を超える場合、その列は単一選択またはドロップダウン単一選択コンポーネントにインポートできません。 | 選択肢A |
| **複数選択**<br />**ドロップダウン複数選択** | Excelファイル内の選択肢が100を超える場合、その列は複数選択またはドロップダウン複数選択コンポーネントにインポートできません。現在、1行につきインポートできる選択肢は1つのみです。選択肢は半角カンマ `,` で区切ります。選択肢の値に `,` が含まれる場合は `,` としてエスケープしてください。 | 選択肢A,選択肢B |
| **日付** | 日付コンポーネントは現在、以下の形式のみに対応しています：<br />• yyyy-MM-dd HH:mm:ss<br />• yyyy-MM-dd HH:mm<br />• yyyy-MM-dd HH<br />• yyyy-MM-dd<br />• yyyy-MM<br />• yyyy | 日付の例：<br />• 2022-12-31 23:50:59<br />• 2022-12-31 23:50<br />• 2022-12-31 23<br />• 2022-12-31<br />• 2022-12<br />• 2022 |
| **日付範囲** | 日付範囲コンポーネントは現在、以下の形式のみに対応しています：<br />• yyyy-MM-dd HH:mm:ss_yyyy-MM-dd HH:mm:ss<br />• yyyy-MM-dd HH:mm_yyyy-MM-dd HH:mm<br />• yyyy-MM-dd HH_yyyy-MM-dd HH<br />• yyyy-MM-dd_yyyy-MM-dd<br />• yyyy-MM_yyyy-MM<br />• yyyy_yyyy | 日付範囲の例：<br />• 2022-12-31 23:50:59_2022-12-31 23:50:59<br />• 2022-12-31 23:50_2022-12-31 23:50<br />• 2022-12-31 23_2022-12-31 23<br />• 2022-12-31_2022-12-31<br />• 2022-12_2022-12<br />• 2022_2022 |
| **部門** | 最上位部門からのフルパスを記入し、親部門と子部門を `-` で区切ります。複数の部門は半角カンマ `,` で区切ります。<br />値はインポート時に検証されます。検証に失敗した行はエラーとして記録されます。 | 部門複数選択：<br />DingTalk-YiDA-YiDA製品,DingTalk-YiDA-YiDA運用<br />部門単一選択：<br />DingTalk-YiDA-YiDA製品 |
| **ユーザー** | ユーザー情報は2つの形式に対応しています：**社員名 + 社員番号** および **社員名 + ユーザー ID**。<br />• 社員番号でインポートする場合は、`名前1(社員番号),名前2(社員番号2)` の形式を使用します。社員番号は社員のプロフィールページで確認できます。<br />• `ユーザー ID` でインポートする場合は、`名前1[ユーザーID],名前2[ユーザーID]` の形式を使用します。ユーザー IDは、[DingTalk管理コンソール](https://oa.dingtalk.io) > **連絡先** > **メンバー管理** > 社員の **基本情報** ページで確認できます。<br />• 退職済み社員は `名前[ユーザーID]` 形式でのみインポート可能です。退職済み社員のユーザー IDは、[DingTalk管理コンソール](https://oa.dingtalk.io) > **連絡先** > **スマート HR** > **退職管理** > 退職済み社員 で確認できます。 | ユーザー IDを使用した例：<br />• **ユーザー複数選択**：`山田太郎[yida777],鈴木花子[yida888],佐藤健[yida999]`<br />• **ユーザー単一選択**：`山田太郎[yida888]`<br />社員番号を使用した例：<br />• **ユーザー複数選択**：`山田太郎(777),鈴木花子(888),佐藤健(999)`<br />• **ユーザー単一選択**：`山田太郎(888)` |
| **添付** | インポートする添付ファイルのURL。URLは以下の要件を満たす必要があります：<br />• `http://` または `https://` で始まること。<br />• 複数のURLは半角カンマ `,` で区切ること。<br />• URLは追加の認証パラメータなしで公開アクセス可能であること。リソースが利用できない場合はセルを空欄にしてください。<br />• ファイル形式はデフォルトでは検証されず、プレビューには対応していません。<br />• インポートとエクスポートの形式は一致しています。例えば、フォームから提出された添付ファイルをエクスポートし、再度一括インポートできます。 | `https://img.alicdn.com/imgextra/i3/O1CN01XcfOQL1Gc4xwrXIP6_!!6000000000642-2-tps-1636-800.png` |
| **画像アップロード** | インポートする画像のURL。画像URLは以下の要件を満たす必要があります：<br />• `http://` または `https://` で始まること。<br />• 複数のURLは半角カンマ `,` で区切ること。<br />• URLは追加の認証パラメータなしで公開アクセス可能であること。リソースが利用できない場合はセルを空欄にしてください。<br />• `.png`、`.jpg`、`.jpeg`、`.gif` 形式のみに対応。プレビューに対応しています。<br />• 検証に失敗した行はエラーとして記録されます。<br />• インポートとエクスポートの形式は一致しています。例えば、フォームから提出された画像をエクスポートし、再度一括インポートできます。 | 画像URL |
| **関連フォーム** | 関連フォームのインポートは2つの形式に対応しています：「**タイトル[インスタンスID]**」および「**メイン情報**」。 | 関連フォームのタイトル[FINST-GO766481LI61UXEQCX2YV7JUGRKW2EVCZD64L0]<br />**または**<br />関連フォームのメイン情報 |

## 注意事項

Excelファイルをアップロードする際は、以下の点にご注意ください。

- ファイルサイズは10 MB以内、単一シートは5,000行以内に収めてください。
- 対応しているファイル形式は `.xls` および `.xlsx` のみです。
- インポート対象のシートのヘッダーに空のセルが含まれていないことを確認してください。含まれている場合、システムはそのシートをインポートしません。
- 組み込み変数による一括インポートデータのフィルタリングには対応していません。
- インポートファイル内の `SUM` や `=H2*J2` などのExcel数式には対応していません。
- インポートした画像や添付ファイルは保存され、添付ファイルのクォータにカウントされます。
- クロス集計のピボットテーブルはインポートできません。

## FAQ

- Q：サブフォームのデータを一括インポートするにはどうすればよいですか？

方法1：サブフォームデータを一括でインポート・エクスポートする新しい方法が利用可能です。詳細は [サブフォーム](/ja/yida/form/in0w72) を参照してください。

方法2：以下のExcelデータ形式を使用します。

例えば、以下に示す在庫フォーム内のサブフォームです。

| **フォーム定義** | フォーム定義の図 ![](https://gw.alicdn.com/imgextra/i3/O1CN018NrLmEr5XoI3pnB3_!!6000000002074-2-tps-1889-888.png) |
|---|---|
| **Excelデータビュー** | Excelデータビューの図 ![](https://gw.alicdn.com/imgextra/i2/O1CN01SnpO3nsGqIC1yjeB_!!6000000004746-2-tps-975-512.png) |
| **インポート済みデータビュー** | インポート済みデータビューの図 ![](https://gw.alicdn.com/imgextra/i3/O1CN01xMfdmexGpbI5pmtK_!!6000000005114-2-tps-2874-1476.png) |
| **フィールドマッピング設定** | フィールドマッピング設定の図 ![](https://gw.alicdn.com/imgextra/i4/O1CN01iS5Ay47cnWL5o1d2_!!6000000003318-2-tps-2860-1484.png) |

- Q：「データをインポート中です。しばらくお待ちください...」と表示され続けるのに、データが追加されないのはなぜですか？

Excelファイル内に数式（`SUM` や `=H2*J2` など）で計算されている列がないか確認してください。数式を含むExcelファイルはインポート対象外であり、強制的にインポートするとさまざまなエラーが発生する可能性があります。

<Frame>
  ![](https://gw.alicdn.com/imgextra/i1/O1CN01HMItAoFlogC5qI8g_!!6000000002991-2-tps-2878-1588.png)
</Frame>

- Q：フォームで設定した形式と異なる場合でも、日付コンポーネントのデータが正常にインポートできるのはなぜですか？

<Frame>
  ![](https://gw.alicdn.com/imgextra/i3/O1CN01AwRgF7rr22E1rYne_!!6000000006757-2-tps-918-576.png)
</Frame>

現在、一括インポートはフォームデザイナーで設定した日付形式に基づいて、日付値の精度を自動的にパディングまたは切り詰めます。以下は例です。

<Frame>
  ![](https://gw.alicdn.com/imgextra/i2/O1CN01MSZaqtFTe6J5m0jU_!!6000000000725-2-tps-2844-1400.png)
</Frame>

インポート結果：

<Frame>
  ![](https://gw.alicdn.com/imgextra/i3/O1CN012H6somwWlPB5nlve_!!6000000002392-2-tps-2858-1136.png)
</Frame>

- Q：一括インポート時に、一部の数値セルがインポートに失敗することがあるのはなぜですか？

A：数値を含むデータをインポートする際、数値コンポーネントを使用する場合は、Excelのセル形式を **数値** または **標準** に設定する必要があります。
