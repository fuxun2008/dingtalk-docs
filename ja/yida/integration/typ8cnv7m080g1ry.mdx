---
title: "データ更新ノード"
description: "DingTalk Yidaの統合と自動化における「データ更新ノード」の2つのモードを解説します。フォームデータの直接更新とノード経由の更新について、設定手順・対応シナリオ・設定境界を紹介します。"
polished: true
---

データノードは統合と自動化の中核機能であり、アプリで生成されたデータに対してさまざまなアクションを実行できます。本記事では、データ更新ノードの使用方法を説明します。

## 機能概要

データ更新ノードは、次の2つの更新モードに対応しています。**フォームデータの直接更新**と**ノード経由でのフォームデータ更新**です。

- フォームデータの直接更新：設定したマッチングルールに基づき、通常フォームのメインテーブルまたはサブフォームを更新します（ソーステーブルは通常フォームまたはワークフローフォームのいずれも指定可能）。直接更新を主眼としたモードです。
- ノード経由でのフォームデータ更新：単一レコード取得ノードまたは複数レコード取得ノードで取得したデータを更新します。まずデータを取得してから更新する流れが特徴です。

本ノードは、統合と自動化フロー・シンプル作業フローのいずれでも利用できます。

## 利用シナリオ

フォームデータの直接更新モードは、1回あたりの更新数に上限がありますが、業務ルールの設定方法に沿って、あるサブフォームから別のサブフォームへ更新できます。ノード経由でのフォームデータ更新モードは従来の更新ノードと同様に動作し、フィルター済みレコードを一括で更新できます。

以下は、フォームデータを直接更新する場合の例です。

<Tip>
**シナリオ：売掛金と実収金の管理**
</Tip>

## 手順

### フォームデータの直接更新

1. Yidaのワークベンチにログインし、マイアプリをクリックします。
2. 対象のアプリを選択し、アプリ設定ページに移動します。
3. 統合と自動化をクリックし、作成済みの統合と自動化フローを選択して、編集をクリックします。

4. 承認ノードのコネクタ線にカーソルを合わせると、**+** ボタンが表示されます。**+** をクリックし、データ更新ノードを選択します。
5. 右側の設定パネルでフォームデータの直接更新を選択し、対象テーブルのタイプをクリックして選択します。

6. データ更新のマッチングルールを設定します。

<Note>
1. マッチングに対応するコンポーネントは、一行テキスト、複数行テキスト、選択、ドロップダウン選択、日付、シリアル番号です。詳細な対応関係については、コンポーネントルールの詳細を参照してください。その他のコンポーネントには未対応です。
2. 一次条件に合致するデータのうち、マッチング可能なレコードは最大100件までです。100件を超えるレコードは更新されません。
</Note>

| メインテーブルの更新 | サブフォームの更新 |
|---|---|

<Steps>
  <Step title="データ更新の更新ルールを設定">
    <Note>
    - 値の代入は、同じタイプのコンポーネント間でのみ可能です。対応コンポーネントは、一行テキスト、複数行テキスト、数値、選択、ドロップダウン選択、チェックボックス、ドロップダウン複数選択、カスケード選択、日付、日付範囲、画像アップロード、添付、ユーザー、住所です。詳細な対応関係については、更新ルールの詳細を参照してください。その他のコンポーネントには未対応です。
    - 直接更新では、メインフォームは最大100件、サブフォームは最大50件までのレコードに対応します。上限を超えるとエラーが発生し、送信は失敗します。並び替えには対応しておらず、マッチした順にレコードが更新されます。
    </Note>
  </Step>
  <Step title="データが一致しない場合のルールを設定します。現在のノードをスキップまたはレコードの追加から選択可能">
  </Step>
  <Step title="保存をクリックして設定を完了">
  </Step>
</Steps>

### ノード経由でのフォームデータ更新

ノード経由でのフォームデータ更新を選択する場合は、現在のノードより前でデータを取得しておく必要があります。詳細については、[単一レコード取得](/ja/yida/integration/svfx3gygmo5yyu6n)または[複数レコード取得](https://www.yuque.com/yida/ugc9hx/gv15lyuaayfc0fyw)を参照してください。

1. Yidaのワークベンチにログインし、マイアプリをクリックします。
2. 対象のアプリを選択し、アプリ設定ページに移動します。
3. 統合と自動化をクリックし、作成済みの統合と自動化フローを選択して、編集をクリックします。
4. 承認ノードのコネクタ線にカーソルを合わせると、**+** ボタンが表示されます。**+** をクリックし、データ更新ノードを選択します。
5. 右側の設定パネルでノード経由でのフォームデータ更新を選択し、更新するデータを選びます。

<Steps>
  <Step title="更新ルールを選択します。左側で対象テーブルのフィールドを選び、右側でマッチング更新ルールを設定">
    <Note>
    注意：更新可能なレコード数は、取得したレコード数に依存します。
    </Note>
  </Step>
  <Step title="データが一致しない場合のルールを設定します。現在のノードをスキップまたはレコードの追加から選択可能">
  </Step>
  <Step title="保存をクリックして設定を完了">
  </Step>
</Steps>

## 設定境界

### マッチングルールの詳細

| **マッチングに対応するコンポーネント** | **タイプ** | **演算子** | **評価可能なフィールドタイプ** |
|---|---|---|---|
| 一行テキスト | テキスト | 等しい<br />等しくない | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号 |
| 複数行テキスト | テキスト | 等しい<br />等しくない | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号 |
| 選択 | テキスト | 等しい<br />等しくない | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号<br />選択<br />ドロップダウン選択 |
| ドロップダウン選択 | テキスト | 等しい<br />等しくない | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号<br />選択<br />ドロップダウン選択 |
| 日付 | タイムスタンプ | 等しい<br />等しくない<br />より前<br />より後<br />以降<br />以前 | 日付 |
| 数値 | 数値 | 等しい<br />等しくない<br />より大きい<br />以上<br />より小さい<br />以下 | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号 |
| シリアル番号 | テキスト | 等しい<br />等しくない | 一行テキスト<br />複数行テキスト<br />数値<br />シリアル番号 |

### 更新ルールの詳細

| **対象コンポーネント（値の代入先）** | **ソースコンポーネント／数式** |
|---|---|
| 一行テキスト | 一行テキスト |
| 複数行テキスト | 複数行テキスト |
| 数値 | 一行テキスト<br />複数行テキスト<br />数値 |
| 選択 | 選択<br />ドロップダウン選択 |
| ドロップダウン選択 | 選択<br />ドロップダウン選択 |
| チェックボックス | チェックボックス |
| ドロップダウン複数選択 | ドロップダウン複数選択 |
| カスケード選択 | カスケード選択 |
| 日付 | 日付 |
| 日付範囲 | 日付範囲 |
| 画像アップロード | 画像アップロード |
| 添付 | 添付 |
| ユーザー | ユーザー |
| 住所 | 住所 |

> ⚠️注意：最終承認者、最終承認時刻、最終承認コメントの3つのフィールドは、手動承認時にのみ生成されます。システムが自動的に実行する場合には生成されません。

## FAQ

**Q：更新ルールの左右のパラメータは、どのように理解すればよいですか？**

A：左側は更新対象となるテーブルのフィールドです。右側はマッチしたソースフィールドです。