# pnpm 11 配置
# - allowBuilds: 允许列表内的包跑 install scripts；esbuild 需在 install 时下载平台原生二进制
# - trustLockfile: 信任已审过的 lockfile，跳过 supply-chain 时延校验
#   避免 electron-to-chromium 等高频发布包卡住 24h、阻塞运营首次安装
allowBuilds:
  esbuild: true
trustLockfile: true
