---
title: "General view settings"
description: "General view settings let Admins define the default View configuration in the admin console. Users in View mode can adjust these settings temporarily but cannot save changes. Admins can Configure filters, column display order, and more to provide a default View for visitors. Users in View mode can temporarily modify settings and Save them, and the latest configuration is displayed when they return. Note: Clearing the browser cache restores the default settings."
polished: true
---
General View settings allow Admins to define the **default View configuration** in the admin console. Users in View mode can make **temporary changes** on top of this baseline, but temporary changes cannot be saved. When Users in View mode return, the default configuration set by the Admin is restored.

### Admin mode

In the app admin console, Admins Configure filters, column display and order, row sorting rules, row height, and other settings, then click **Save** to provide the default View configuration for View mode.

Grid Views support four row-height styles: Low, Medium, High, and Extra high. Configure the row height for each View.

### View mode

Based on the Admin's default configuration, Users in View mode can make temporary changes to filters, column display and order, row sorting rules, row height, and other settings. Temporary changes can be saved, and the latest View-mode configuration is displayed when the User returns.

<Warning>
After an **app visitor** refreshes the page, the current form order is preserved. To restore the default settings (that is, the order defined by the app editor), open the browser and clear the cache. The steps below use Chrome as an example.
</Warning>

#### Clear cached data