---
title: "ARRAYGET"
description: "The ARRAYGET function returns the value at a specified position in a dataset. Use it in various YiDA scenarios such as forms and workflow design. This function requires two parameters—the dataset and the element position—and returns Text type data."
polished: true
---

The ARRAYGET function returns the value at a specified position in a dataset.

## Supported product scenarios

The ARRAYGET function is supported in the following YiDA scenarios:

- Form (main table fields)
- Form (business association rules)
- Form (validation)
- Workflow design (validation rules)
- Workflow design (associated actions)
- Integration and automation
- Workflow node
- Connector

## Syntax

```text
ARRAYGET(array, k)
```

## Attribute description

| **Attribute** | **Required** | **Description** |
|---|---|---|
| array | Yes | The dataset from which to retrieve data. Array type. |
| k | Yes | The position of the element in the dataset, starting from 1. |

## Return value

| **Return type** | **Description** |
|---|---|
| Text | The value at the specified position in the dataset. |

## Example

## Try it

Returns the third record from multiple detail entries in a subform.