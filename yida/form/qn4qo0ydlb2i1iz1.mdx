---
title: "NOW"
description: "The NOW function returns the current time and works across multiple YiDA scenarios, including Forms and workflow design. It returns a Date object that can be assigned to a Date component."
polished: true
---
The **NOW** function returns the current time.

## Supported Scenarios

The NOW function is supported in the following YiDA scenarios.

- Form (main table fields)
- Form (business association rules)
- Form (validation)
- Workflow design (validation rules)
- Workflow design (association actions)
- Integration & automation
- Workflow node
- Connector

## Syntax

```text
NOW()
```

## Return Value

| **Return type** | **Description** |
|---|---|
| Date object | Returns the current time as a Date object. |

## Example

As shown below, use the `NOW()` function to assign the current time to a **Date component**.

<Frame>
  ![](https://gw.alicdn.com/imgextra/i2/O1CN01BkxKLAOrvLK1daPY_!!6000000000191-2-tps-807-289.png)
</Frame>

## Notes

Returns the current time. Produces the same result as the `TODAY` function.