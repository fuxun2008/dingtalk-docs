---
title: "Storage configuration"
description: "Yida Dedicated edition offers private storage options for business data, letting you choose either a public cloud or a dedicated hybrid cloud database for new or existing apps. Once dedicated storage is enabled, app data is stored in a dedicated PostgreSQL database and a dedicated attachment OSS. The process takes 3–10 minutes and is irreversible."
polished: true
---

Dedicated edition only

| Feature | Free plan | Basic edition | Professional edition | Dedicated edition |
|---|---|---|---|---|
| Dedicated storage and dedicated Security | Not supported | Not supported | Not supported | Supported |

Yida Dedicated edition provides enhanced customization for business data storage. Choose a more autonomous, secure, and controllable approach to keep business data in private storage.

## Enable on demand (app level)

### 1. Enable when creating a new app

When creating a new app, Select whether the app's business data is stored in the public cloud (default) or in a dedicated hybrid cloud database (recommended for core critical data).

### 2. Enable manually for existing apps

- As the App Admin, go to App Settings > Basic Settings > Dedicated Configuration in the app backend, then Select and enable dedicated storage for the app.
- After you enable it, both historical data and future data for that app are stored in the dedicated PostgreSQL database and dedicated attachment OSS configured in platform management.
- App functionality is suspended during the upgrade, so perform this Action during off-peak hours. The process takes about 3–10 minutes and cannot be canceled after completion. You can manually stop the upgrade while it is in progress.

### 3. Upgrade and stop operations

- Changing the app's basic framework configuration is a time-consuming operation. To stop the process midway, halt it manually. After you stop the upgrade, the app's basic configuration reverts to the original cloud storage Settings and operating mode.
- Factors that affect upgrade duration: