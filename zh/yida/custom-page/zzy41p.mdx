---
title: "循环渲染"
description: "了解如何使用宜搭的循环渲染功能，包括静态循环数据、远程数据源循环以及嵌套循环的应用场景与设置方法。"
---

| **能力** | **免费版** | **轻享版** | **专业版** | **专属版** |
|---|---|---|---|---|
| 自定义页面 | 不支持 | 不支持 | 支持 | 支持 |

## 1. 概念说明

对于需要循环渲染的场景，宜搭也准备了循环渲染设置能力

循环数据要求是数组结构，字符串数组、数字数组或者对象数组都可以

## 2. 使用场景案例

### 场景一：静态循环数据

「循环数据」定义一个静态数组，在循环体里，可以通过 this.item 获取循环值，this.index 获取循环下标。当然，item / index 也可以通过编辑「循环参数」来自定义，比如 content / idx

在【循环数据】中编辑数据，录入循环数据

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01zDErmMI6yiE3IBKR_!!6000000002145-2-tps-1622-841.png)
</Frame>

表单编辑页面

在需要的设置中绑定循环变量

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN014NFJjA84knC3IYk4_!!6000000006375-2-tps-1625-839.png)
</Frame>

绑定变量

效果图如下：

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01jiqlE2jG3fK5pWzk_!!6000000007692-2-tps-2872-372.png)
</Frame>

### 场景二：循环远程数据源

创建一个远程数据源

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN01EGPMXPdTvbL3HAtK_!!6000000007382-2-tps-1614-840.png)
</Frame>

将数据源绑定到循环变量上：

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01jc7jvGctzkI3Hnuq_!!6000000004819-2-tps-1619-845.png)
</Frame>

然后绑定循环变量中的值到组件中：

<Frame>
  ![](https://img.alicdn.com/imgextra/i1/O1CN01bfpzzTbxeXI3I3WV_!!6000000003608-2-tps-1621-838.png)
</Frame>

跟场景一比起来，主要多了一个定义远程数据源和将组件的循环数据绑定到该数据源上

效果图如下：

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01kuFOvWozdGB5pmvA_!!6000000004461-2-tps-2874-1590.png)
</Frame>

### 场景三：嵌套循环

比如远程数据源返回的数据结构如下：

```javascript
[
  {
    type: '我是标题一',
    detail: [
      '内容一',
      '内容二',
      '内容三'
    ]
	},
  {
    type: '我是标题一',
    detail: [
      '内容一',
      '内容二',
      '内容三'
    ]
	},
  ...
]
```

那么我们可以实现内外两层嵌套循环，以此类推，其实 N 层（N \< Infinity）也是支持的

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01m67lwLZ1nmG3Hvii_!!6000000003164-2-tps-1620-844.png)
</Frame>

通常对于两个组件搭配到一起应用嵌套循环时，我们会在外层包裹一层容器组件，在容器组件上设置外层循环数据，在内部的组件（此例为文本组件）设置内层循环数据

外层的设置方式跟场景二一样，关键看下内层怎么设置：

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN01vnniidI4KZK3HAtN_!!6000000007714-2-tps-1614-843.png)
</Frame>

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN017nHXPCYcxqB1GIds_!!6000000000062-2-tps-622-626.png)
</Frame>

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01oy8TIeBgvoB3HIhF_!!6000000006839-2-tps-1615-842.png)
</Frame>

细心的你一定发现，为什么外层循环值用的是 this.item，而内层用的是 this.content，这是故意这样设置（通过编辑内层的「循环参数」），为了在内层也能拿到外层的值，否则两个都是 this.item，内层会将外层屏蔽。

效果如图：

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN012LcrLjBG9kB5pHha_!!6000000004570-2-tps-2870-1588.png)
</Frame>

## 3. 注意事项

### 3.1 关于循环中的 this 使用

在整个搭建页面中，this 其实是有层级的，当前作用域是 this，父级作用域是 this.$super，顶级作用域是 this.$top。

比如对表单容器做循环渲染，表单标识为 'fooForm'，在 **循环体** 里使用 `this.$('fooForm').getValue()` 可以拿到当次循环表单的值，若要获取所有循环表单的值，需要通过 `this.$super.$('fooForm').map(item => item.getValue())`
