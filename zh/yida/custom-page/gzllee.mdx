---
title: "步骤条"
description: "页面介绍了步骤条组件的使用场景、基础属性配置及高级配置，包括当前步骤属性、类型属性、展示方向等，并提供了动态设置步骤数据的方法和典型案例。"
---

| **能力** | **免费版** | **轻享版** | **专业版** | **专属版** |
|---|---|---|---|---|
| 步骤条 | 不支持 | 不支持 | 支持 | 支持 |

步骤条组件是自定义页面的高级组件。

## 1. 使用场景

自定义页面的步骤条组件，适用于制作派遣人员信息录入，登录信息注册等，可以对基本信息的填写进度作提醒。

<Note>
组件属性以及使用和示例请[**点击此处**](/zh/open/yida/components/advanced/steps)查看
</Note>

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN019UR1fygyhUJ2tNlx_!!6000000005528-1-tps-1425-672.gif)
</Frame>

步骤条演示

## 2. 组件基础属性

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01XW07SzQClSF2v91e_!!6000000002865-2-tps-1439-627.png)
</Frame>

步骤组件属性设置

### 2.1 当前步骤属性

数字类型，控制当前步骤所在步骤数据的状态，如果步骤数据的状态都是默认，那么当前步骤就会显示进行中的状态，之前的步骤显示已完成，后面的步骤显示等待。

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01YF1oRpLS5XG2v8xv_!!6000000004497-2-tps-1439-396.png)
</Frame>

当前步骤属性

### 2.2 类型属性

步骤条的类型分为cricle圆型，arrow箭型，dot点型，如图为以下三种样式。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01lCJtTj6f4SL2v94U_!!6000000007776-2-tps-1439-803.png)
</Frame>

步骤条类型属性样式

### 2.3 展示方向

步骤条的展示方向可以设置水平和垂直，其中箭型不能设置垂直样式。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01llrxgjP5zxK2v1Gd_!!6000000001658-2-tps-1438-805.png)
</Frame>

步骤条展示方向属性样式

### 2.4 内容排列属性

内容排列属性只有cricle类型支持，箭型和点型不支持这个属性，所以选择了这两种类型后不会。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01SQC7oFeGg2L2v1CC_!!6000000000599-2-tps-1438-530.png)
</Frame>

步骤条内容排列属性样式

### 2.5 只读模式

默认关闭只读模式，允许点击步骤节点，可以执行在步骤节点设置的自定义动作。

开启只读默认，不允许点击步骤节点，不会触发自定义动作。

设置节点的自定义动作：

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN01zzziA1RKA1G2tNlx_!!6000000000798-1-tps-1425-672.gif)
</Frame>

步骤条自定义动作

<Note>
以下代码可以直接复制到JS面板
</Note>

```javascript
export function onClick(index) {
  this.utils.toast({
    title: `当前点击的index：${index}`
  });
  if(index == 0){
    this.utils.router.push('https://www.yidaapps.com', {}, true, true)
  }
}
```

## 3. 组件高级配置

### 3.1 数据格式

| **字段** | **名称** | **说明** |
|---|---|---|
| title | 标题 |  |
| content | 内容 | 如不传，会根据外层的 Step 的 当前步骤(current) 属性生成，可选值为 wait, process, finish, |
| status | 状态 |  |
| icon | 图标 |  |
| percent | 百分比 |  |
| disabled | 是否禁用 |  |

```javascript
[
    {
        "title": "step1",
        "content": "内容",
        "status": "wait",
        "icon": "smile",
        "percent": 100,
        "disabled": false,
    },
    {
        "title": "step2",
        "content": "内容",
        "status": "wait",
        "icon": "smile",
        "percent": 100,
        "disabled": false,
    },
]
```

### 3.2 动态设置步骤数据

步骤条的步骤数据绑定变量数据源，拖动一个按钮组件，设置点击动作。当点击时更改变量数据源内的值来实现动态设置步骤数据的功能。

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01ANdgYpmxBUH2v1GC_!!6000000002542-2-tps-1438-778.png)
</Frame>

绑定变量数据源

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN01OILY8IiVBTK2tNlx_!!6000000003327-1-tps-1425-672.gif)
</Frame>

设置按钮组件的动作

<Note>
以下代码可以直接复制到 JS 面板
</Note>

```javascript
//添加一项绑定动作设置
export function onClickAdd(){
  console.log('onClick');
  const data = this.state.demoData
  data.push({
    'title': 'newStep',
    'content': '新增内容'
  })
  this.setState({
    demoData: data,
    currentData: this.state.currentData + 1
  })
}
```

### 3.3 典型案例

设置变量数据源，默认值为 0。给步骤条的当前步骤属性绑定变量数据源。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01O2WNOFWNKJE2v93G_!!6000000002499-2-tps-1439-727.png)
</Frame>

绑定变量数据源

#### 3.3.1 准备操作

##### （1）设置容器组件的唯一标识，将需要填写的组件数据放置在容器组件中，根据容器组件的显示隐藏控制下一步需要填写的组件显隐。

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN012ADjTyRoK6D2v94Z_!!6000000004890-2-tps-1439-808.png)
</Frame>

设置容器组件唯一标识

##### （2）设置提交成功/失败的提示文本、图标和样式，这里使用变量数据源控制容器组件显隐

<Frame>
  ![](https://img.alicdn.com/imgextra/i1/O1CN01bjCWqFmrhID2v1Gg_!!6000000007803-2-tps-1438-808.png)
</Frame>

设置数据源控制容器显隐

<Frame>
  ![](https://img.alicdn.com/imgextra/i2/O1CN01mW6vL3PAtQC2ioCT_!!6000000006653-1-tps-1341-736.gif)
</Frame>

block_3容器绑定变量数据源

##### （3）给按钮组件绑定动作，其中下一步，重新提交和再次提交按钮共用一个 onclick 动作。

<Frame>
  ![](https://img.alicdn.com/imgextra/i1/O1CN01H3BT8h6eJyK2utSm_!!6000000003412-2-tps-1437-807.png)
</Frame>

按钮组件绑定动作

##### （4）步骤节点设置自定义点击动作 onclick2

<Frame>
  ![](https://img.alicdn.com/imgextra/i4/O1CN01pkL7a4uCarF2tNlx_!!6000000002194-1-tps-1425-672.gif)
</Frame>

步骤条绑定动作+效果演示

<Note>
以下代码可以直接复制到 JS 面板，注意：需要替换组件的唯一标识
</Note>

```javascript
/**
* button onClick
*/
export function onClick() {
  let formCurrent = this.state.formCurrent
  switch (formCurrent) {
    //第一次点击formCurrent是0
    case 0:
        this.$("block_1").set("behavior", "HIDDEN")
        this.$("block_2").set("behavior", "NORMAL")
        this.setState({
          formCurrent: formCurrent + 1
        })
      break;
    case 1:
        this.$("block_2").set("behavior", "HIDDEN")
        //随机成功或者失败。后续可以调用数据源用请求的返回结果判断成功或者失败。
        const isSuccess = Math.random() < 0.5 ? "block_3_type" : "block_4_type";
        console.log(isSuccess)
        this.setState({
          formCurrent: formCurrent + 1,
          [isSuccess]: "NORMAL",
          action: "HIDDEN"
        })
      break;
    case 2:
    //将容器1组件值重置为空
      this.$("textField_ktjfxslga").reset()
      this.$("textField_ktjfxslh").reset()
      this.$("textField_ktjfxslj").reset()
      this.$("textField_ktjfxslk").reset()
      this.$("textareaField_ktjfxslm").reset()
    //将容器2组件值重置为空
      this.$("textField_ktjfxsln").reset()
      this.$("textField_ktjfxslo").reset()
      this.$("textField_ktjwwsj5").reset()
      this.$("textareaField_ktjfxsls").reset()

    //将容器1显示，容器2隐藏
      this.$("block_1").set("behavior", "NORMAL")
      this.$("block_2").set("behavior", "HIDDEN")
      this.setState({
        formCurrent: 0,
        action: "NORMAL",
        block_3_type: "HIDDEN",
        block_4_type: "HIDDEN"
      })
      break;
  }
}

/**
 * FusionStep onClick
 * @param value 节点索引
 */
export function onClick2(index) {
  this.utils.toast({
    title: `当前点击的index：${index}`
  });
  if(index == 0){
    this.setState({
      formCurrent: 0,
      action: "NORMAL",
      block_3_type: "HIDDEN",
      block_4_type: "HIDDEN"
    })
    this.$("block_1").set("behavior", "NORMAL")
    this.$("block_2").set("behavior", "HIDDEN")
  }
}
```
