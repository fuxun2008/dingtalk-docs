---
title: "POWER"
description: "POWER函数用于计算某个数的乘幂，支持在宜搭的不同场景中使用，如表单、流程设计等。了解其格式、属性说明及如何应用。"
---

**POWER**函数用于计算某个数的乘幂。

## 支持产品场景

POWER函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）
- 集成自动化
- 流程-节点
- 连接器

## 格式

```text
POWER(number,power)
```

## 属性说明

| 属性 | 是否必填 | 说明 |
|---|---|---|
| number | 是 | 数字类型，基数，可为任意实数。 |
| power | 否 | 数字类型，乘幂运算中的指数。 |

## 返回值

| 返回值类型 | 说明 |
|---|---|
| 数值 | 乘幂后的值。 |

## 使用示例

如下图所示，使用`POWER()`函数计算`2`的`10`次方的值。

<Frame>
  ![](https://yida-support.oss-cn-shanghai.aliyuncs.com/static/png/1704279660796-d70af13c-fa02-4bb3-b840-a30a7234ab99.png)
</Frame>

## 体验

计算出数值组件乘幂的值。
