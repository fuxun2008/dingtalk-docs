---
title: "OR"
description: "OR函数在宜搭中的使用说明，包括支持的产品场景、格式、属性说明、返回值以及使用示例。适用于表单校验、流程设计等多种场景。"
---

**OR**函数表示或。函数中的所有逻辑表达式，只要有一个为true，OR函数的结果就为true。

## 支持产品场景

OR函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）
- 集成自动化
- 流程-节点
- 连接器

## 格式

```text
OR(condition1,condition2....condition.N)
```

## 属性说明

<table>
  <tr>
    <td>**属性**</td>
    <td>**是否必填**</td>
    <td>**说明**</td>
  </tr>
  <tr>
    <td>condition1</td>
    <td>是</td>
    <td rowSpan={2}>需要评估的逻辑表达式或条件。<br />用于判断两个或多个条件是否有一个为真（即是否有一项满足特定条件）。<br /><ul><li>如果所有的条件都为假（逻辑值为false），那么OR函数的返回false。</li><li>如果有任何一个条件为真（逻辑值为true），那么OR函数返回true。</li></ul></td>
  </tr>
  <tr>
    <td>condition2</td>
    <td>是</td>
  </tr>
</table>

## 返回值

| **返回值类型** | **说明** |
|---|---|
| 布尔值 | 函数中的所有逻辑表达式，只要有一个为true，OR函数的结果就为true。 |

## 使用示例

如下图所示，`数值1`和`数值2`的值只要有一个大于 2 ，`OR()`函数的返回值就为**true**，否则为**false**。

<Frame>
  ![](https://img.alicdn.com/imgextra/i1/O1CN01eB4iUBLjDLE1roRG_!!6000000004581-1-tps-920-690.gif)
</Frame>

## 体验

有组件中有任意一个组件满足条件阻断提交表单。
