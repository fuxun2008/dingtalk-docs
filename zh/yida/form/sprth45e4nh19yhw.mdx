---
title: "LOGINUSERWORKNO"
description: "LOGINUSERWORKNO函数用于获取当前登录成员在当前组织的唯一ID（userId），支持在宜搭的多种场景中使用，如表单和流程设计等。"
---

**LOGINUSERWORKNO**函数用于获取当前登录成员在当前组织的唯一ID（userId）。

## 支持产品场景

LOGINUSERWORKNO函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）

## 格式

```text
LOGINUSERWORKNO()
```

## 返回值

| **返回值类型** | **说明** |
|---|---|
| 文本 | 返回的当前登录员工的在当前组织的唯一ID（userId）。 |

## 使用示例

如下图所示，使用`LOGINUSERWORKNO()`函数获取当前登录员工的唯一ID（userId）。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01TogwROnV0DI1qnyS_!!6000000005759-1-tps-912-584.gif)
</Frame>
