---
title: "GT"
description: "GT函数用于计算两个值的其中一个是否大于另一个值，支持在宜搭的多种场景中使用，如表单、流程设计和集成自动化等。通过比较两个数字或日期类型的值，返回布尔值表示比较结果。"
---

GT函数用于计算两个值的其中一个是否**大于**另一个值。

## 支持产品场景

GT函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）
- 集成自动化
- 流程-节点
- 连接器

## 格式

```text
GT(value1,value2)
```

## 属性说明

| **属性** | **是否必填** | **说明** |
|---|---|---|
| value1 | 是 | 进行比较的值，支持数字、日期。 |
| value2 | 是 | 进行比较的值，支持数字、日期。 |

## 返回值

| **返回值类型** | **说明** |
|---|---|
| 布尔值 | 两个参数比较后的结果，取值。<br />• **true**：如果返回true，则表示value1大于value2。<br />• **false**：如果返回false，则表示value2小于等于value1。 |

## 使用示例

如下图所示，使用`GT()`函数比较`数值 1`和`数值 2`的大小，当`数值 1`的值大于`数值 2`的值时，返回true。

<Frame>
  ![](https://img.alicdn.com/imgextra/i3/O1CN01RuS2rFKgyLG1rYoq_!!6000000001161-1-tps-918-650.gif)
</Frame>
