---
title: "MAX"
description: "了解如何在宜搭的不同场景中使用MAX函数来获取一组数字的最大值，包括表单、流程设计和集成自动化等。"
---

MAX函数用于获取一组数字的最大值。

## 支持产品场景

MAX函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）
- 集成自动化
- 流程-节点
- 连接器

## 格式

```text
MAX(number1,number2···numberN)
```

## 属性说明

| 属性 | 是否必填 | 说明 |
|---|---|---|
| number | 是 | 数字类型，需要计算最大值的一组数字，多个数之间使用英文逗号分隔。 |

## 返回值

| 返回值类型 | 说明 |
|---|---|
| 数值 | 通过计算得到的最大值。 |

## 使用示例

如下图所示，使用`MAX()`函数获取一组数字的最大值。

<Frame>
  ![](https://yida-support.oss-cn-shanghai.aliyuncs.com/static/png/1704252520005-45a331ab-7a3b-46d1-917a-57a44579100e.png)
</Frame>

## 体验

返回数值组件中值最大的数据。
