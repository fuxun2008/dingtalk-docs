---
title: "SQRT"
description: "SQRT函数用于计算某个数的正平方根，支持在宜搭的多种场景中使用，如表单、流程设计等。通过输入数字作为参数，返回该数字的正平方根。"
---

**SQRT**函数用于计算某个数的正平方根。

## 支持产品场景

SQRT函数支持在宜搭的以下场景中使用。

- 表单（主表字段）
- 表单（业务关联规则）
- 表单（校验）
- 流程设计（校验规则）
- 流程设计（关联操作）
- 集成自动化
- 流程-节点
- 连接器

## 格式

```text
SQRT(number)
```

## 属性说明

| **属性** | **是否必填** | **说明** |
|---|---|---|
| number | 是 | 数字类型，需要计算平方根的数。 |

## 返回值

| **返回值类型** | **说明** |
|---|---|
| 数值 | 返回的正平方根。 |

## 使用示例

如下图所示，使用`SQRT()`函数计算`9`的正平方根。

<Frame>
  ![](https://yida-support.oss-cn-shanghai.aliyuncs.com/static/png/1704285743778-03ec8c56-8f78-49b4-9bf6-053d437abd81.png)
</Frame>

## 体验

获取数值组件值正的平方根。
