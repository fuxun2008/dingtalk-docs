# Mintlify 站点构建忽略（语法同 .gitignore）
# 排除非文档资源，避免 mint dev / 构建时扫描报无意义 warning

# 校对工具子项目（独立 Vite + React 应用，不是 mdx 内容）
tools/

# 翻译辅助脚本与术语表
scripts/

# 项目级 skill / 内部记录
.claude/

# Qoder repowiki 生成物（非站点内容，含无法解析的 md）
.qoder/
