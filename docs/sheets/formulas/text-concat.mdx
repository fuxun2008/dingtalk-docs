---
title: "Text concatenation functions"
description: "| Tag | Concatenation, &,<br>CONCAT, TEXTJOIN |"
---

| Tag | Concatenation, &,<br />CONCAT, TEXTJOIN |
| --- | --- |

In DingTalk Spreadsheet, you can concatenate text using several methods.

## Instructions

## Method 1: The "&" concatenation operator

Figure 1

## Method 2: CONCATENATE and CONCAT functions

The CONCATENATE function joins multiple text strings together, with each argument representing one text string. For example:

Figure 2

CONCAT is a more flexible text concatenation function. Each argument can be a single text string or a Quote that refers to multiple text strings:

Figure 3

## Method 3: TEXTJOIN function

The TEXTJOIN function also joins multiple text strings, inserting a specified delimiter between each pair of strings.

The arguments of the TEXTJOIN function are, in order:

*   **Delimiter (required). If left empty, the result is the same as the CONCATENATE function.**

*   **Whether to ignore empty text.**

*   **One or more arguments specifying the text to join. At least one is required.**

---

▍Back to the "[Spreadsheet](/zh/docs/sheets/index)" Table of contents
