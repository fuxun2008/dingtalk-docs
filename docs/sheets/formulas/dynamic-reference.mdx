---
title: "Dynamic reference function"
description: "The INDIRECT function parses text into a range reference. In most formulas, references are typically entered directly, such as SUM(A1:A5), where A1:A5 is not wrapped in quotation marks and directly represents the range A1:A5."
---

## INDIRECT function

The INDIRECT function parses text into a range reference. In most formulas, references are typically entered directly, such as SUM(A1:A5), where A1:A5 is not wrapped in quotation marks and directly represents the range A1:A5.

INDIRECT is generally used in combination with the concatenation operator "&" or text functions. For example:

|  |
| --- |
|  |
|  |

In Figure 1, the text concatenated by the INDIRECT parameter in Cell C1 of the "Summary" sheet is "Apples!B:B".

After being parsed by the INDIRECT function, it references Column B of the "Apples" sheet, and the SUM function then calculates the "Total sales".

---

▍Back to the「[Spreadsheet](/en/docs/sheets/index)」table of contents
