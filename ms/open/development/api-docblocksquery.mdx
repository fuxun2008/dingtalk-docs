---
title: "Pertanyaan elemen blok"
description: "Panggil API ini untuk pertanyaan elemen blok peringkat pertama di bawah root dokumen, dengan penapisan mengikut kedudukan mula/akhir dan jenis blok."
---

Panggil API ini untuk menanyakan elemen blok peringkat pertama di bawah dokumen, iaitu elemen blok yang terus berada di bawah nod root. Pertanyaan ini menyokong penapisan mengikut kedudukan mula/akhir dan jenis blok.

## Penerangan panggilan API

Untuk maklumat terperinci tentang struktur data yang berkaitan dengan kandungan dokumen, lihat [Struktur data dokumen](/ms/open/development/document-data-structure).

## Permintaan

### Maklumat asas

| Medan | Nilai |
| --- | --- |
| URL HTTP | `https://api.dingtalk.io/v1.0/doc/suites/documents/{docKey}/blocks` |
| Kaedah HTTP | GET |
| Jenis aplikasi disokong | appType-Aplikasi dalaman |
| Kebenaran diperlukan | permission-Storage.File.Read-Kebenaran Telah dibaca untuk fail storan organisasi |

### Header permintaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| x-acs-dingtalk-access-token | string | Ya | Kredensial akses yang digunakan untuk memanggil API ini. Dapatkan dengan memanggil API [Dapatkan access token aplikasi dalaman](/ms/open/development/obtain-the-access-token-of-an-internal-app). |

### Parameter laluan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| docKey | string | Ya | ID Dokumen (`docKey` atau `dentryUuid` bagi dokumen tersebut). Selepas anda mencipta dokumen dengan memanggil API [Cipta dokumen Knowledge Base](/ms/open/development/create-team-space-document), dapatkan `docKey` daripada respons. Sebagai alternatif, dapatkan `dentryUuid` daripada URL dokumen. Sebagai contoh, dalam https://docs.dingtalk.io/i/nodes/Zxxxxa-id, `dentryUuid` ialah Zxxxxa-id. |

### Parameter pertanyaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| startIndex | integer | Tidak | Kedudukan mula. Integer yang lebih besar daripada atau sama dengan 0, menunjukkan bahawa pertanyaan bermula daripada elemen blok ke-`startIndex` di bawah dokumen. |
| endIndex | integer | Tidak | Kedudukan akhir. Integer yang lebih besar daripada atau sama dengan 0, menunjukkan bahawa pertanyaan berakhir pada elemen blok ke-`endIndex`. |
| blockType | string | Tidak | Jenis elemen blok yang ditentukan. Untuk maklumat terperinci, lihat [Struktur data elemen blok](/ms/open/development/block-data-structure). |
| operatorId | string | Ya | unionId pengguna. Dapatkan nilai parameter `unionid` dengan memanggil API [Pertanyaan butiran pengguna](/ms/open/development/query-user-details). |

### Contoh permintaan

HTTP

```http
GET /v1.0/doc/suites/documents/doc_id/blocks?startIndex=0&endIndex=1&blockType=paragraph&operatorId=union_id HTTP/1.1
Host:api.dingtalk.io
x-acs-dingtalk-access-token:7dc9ec4xxx
Content-Type:application/json
```

Java

```java
package com.aliyun.sample;

import com.aliyun.tea.*;

public class Sample {

    /**
     * <b>description</b> :
     * <p>Initialize the account Client with a Token</p>
     * @return Client
     * 
     * @throws Exception
     */
    public static com.aliyun.dingtalkdoc_1_0.Client createClient() throws Exception {
        com.aliyun.teaopenapi.models.Config config = new com.aliyun.teaopenapi.models.Config();
        config.protocol = "https";
        config.regionId = "central";
        return new com.aliyun.dingtalkdoc_1_0.Client(config);
    }

    public static void main(String[] args_) throws Exception {
        
        com.aliyun.dingtalkdoc_1_0.Client client = Sample.createClient();
        com.aliyun.dingtalkdoc_1_0.models.DocBlocksQueryHeaders docBlocksQueryHeaders = new com.aliyun.dingtalkdoc_1_0.models.DocBlocksQueryHeaders();
        docBlocksQueryHeaders.xAcsDingtalkAccessToken = "<your access token>";
        com.aliyun.dingtalkdoc_1_0.models.DocBlocksQueryRequest docBlocksQueryRequest = new com.aliyun.dingtalkdoc_1_0.models.DocBlocksQueryRequest()
                .setStartIndex(0)
                .setEndIndex(1)
                .setBlockType("paragraph")
                .setOperatorId("union_id");
        try {
            client.docBlocksQueryWithOptions("doc_id", docBlocksQueryRequest, docBlocksQueryHeaders, new com.aliyun.teautil.models.RuntimeOptions());
        } catch (TeaException err) {
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err contains the code and message attributes, which help locate the issue
            }

        } catch (Exception _err) {
            TeaException err = new TeaException(_err.getMessage(), _err);
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err contains the code and message attributes, which help locate the issue
            }

        }        
    }
}
```

Python

```python
# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
import os
import sys
import json

from typing import List

from alibabacloud_dingtalk.doc_1_0.client import Client as dingtalkdoc_1_0Client
from alibabacloud_tea_openapi import models as open_api_models
from alibabacloud_dingtalk.doc_1_0 import models as dingtalkdoc__1__0_models
from alibabacloud_tea_util import models as util_models
from alibabacloud_tea_util.client import Client as UtilClient

class Sample:
    def __init__(self):
        pass

    @staticmethod
    def create_client() -> dingtalkdoc_1_0Client:
        """
        Initialize the account Client with a Token
        @return: Client
        @throws Exception
        """
        config = open_api_models.Config()
        config.protocol = 'https'
        config.region_id = 'central'
        return dingtalkdoc_1_0Client(config)

    @staticmethod
    def main(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        doc_blocks_query_headers = dingtalkdoc__1__0_models.DocBlocksQueryHeaders()
        doc_blocks_query_headers.x_acs_dingtalk_access_token = '<your access token>'
        doc_blocks_query_request = dingtalkdoc__1__0_models.DocBlocksQueryRequest(
            start_index=0,
            end_index=1,
            block_type='paragraph',
            operator_id='union_id'
        )
        try:
            client.doc_blocks_query_with_options('doc_id', doc_blocks_query_request, doc_blocks_query_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err contains the code and message attributes, which help locate the issue
                pass

    @staticmethod
    async def main_async(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        doc_blocks_query_headers = dingtalkdoc__1__0_models.DocBlocksQueryHeaders()
        doc_blocks_query_headers.x_acs_dingtalk_access_token = '<your access token>'
        doc_blocks_query_request = dingtalkdoc__1__0_models.DocBlocksQueryRequest(
            start_index=0,
            end_index=1,
            block_type='paragraph',
            operator_id='union_id'
        )
        try:
            await client.doc_blocks_query_with_options_async('doc_id', doc_blocks_query_request, doc_blocks_query_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err contains the code and message attributes, which help locate the issue
                pass

if __name__ == '__main__':
    Sample.main(sys.argv[1:])
```

PHP

```java
<?php

// This file is auto-generated, don't edit it. Thanks.
namespace AlibabaCloud\SDK\Sample;

use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Dingtalk;
use \Exception;
use AlibabaCloud\Tea\Exception\TeaError;
use AlibabaCloud\Tea\Utils\Utils;

use Darabonba\OpenApi\Models\Config;
use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Models\DocBlocksQueryHeaders;
use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Models\DocBlocksQueryRequest;
use AlibabaCloud\Tea\Utils\Utils\RuntimeOptions;

class Sample {

    /**
     * Initialize the account Client with a Token
     * @return Dingtalk Client
     */
    public static function createClient(){
        $config = new Config([]);
        $config->protocol = "https";
        $config->regionId = "central";
        return new Dingtalk($config);
    }

    /**
     * @param string[] $args
     * @return void
     */
    public static function main($args){
        $client = self::createClient();
        $docBlocksQueryHeaders = new DocBlocksQueryHeaders([]);
        $docBlocksQueryHeaders->xAcsDingtalkAccessToken = "<your access token>";
        $docBlocksQueryRequest = new DocBlocksQueryRequest([
            "startIndex" => 0,
            "endIndex" => 1,
            "blockType" => "paragraph",
            "operatorId" => "union_id"
        ]);
        try {
            $client->docBlocksQueryWithOptions("doc_id", $docBlocksQueryRequest, $docBlocksQueryHeaders, new RuntimeOptions([]));
        }
        catch (Exception $err) {
            if (!($err instanceof TeaError)) {
                $err = new TeaError([], $err->getMessage(), $err->getCode(), $err);
            }
            if (!Utils::empty_($err->code) && !Utils::empty_($err->message)) {
                // err contains the code and message attributes, which help locate the issue
            }
        }
    }
}
$path = __DIR__ . \DIRECTORY_SEPARATOR . '..' . \DIRECTORY_SEPARATOR . 'vendor' . \DIRECTORY_SEPARATOR . 'autoload.php';
if (file_exists($path)) {
    require_once $path;
}
Sample::main(array_slice($argv, 1));
```

Go

```go
package main

import (
  "encoding/json"
  "strings"
  "fmt"
  "os"
  util  "github.com/alibabacloud-go/tea-utils/v2/service"
  dingtalkdoc_1_0  "github.com/alibabacloud-go/dingtalk/doc_1_0"
  openapi  "github.com/alibabacloud-go/darabonba-openapi/v2/client"
  "github.com/alibabacloud-go/tea/tea"
)

// Description:
// 
// Initialize the account Client with a Token
// 
// @return Client
// 
// @throws Exception
func CreateClient () (_result *dingtalkdoc_1_0.Client, _err error) {
  config := &openapi.Config{}
  config.Protocol = tea.String("https")
  config.RegionId = tea.String("central")
  _result = &dingtalkdoc_1_0.Client{}
  _result, _err = dingtalkdoc_1_0.NewClient(config)
  return _result, _err
}

func _main (args []*string) (_err error) {
  client, _err := CreateClient()
  if _err != nil {
    return _err
  }

  docBlocksQueryHeaders := &dingtalkdoc_1_0.DocBlocksQueryHeaders{}
  docBlocksQueryHeaders.XAcsDingtalkAccessToken = tea.String("<your access token>")
  docBlocksQueryRequest := &dingtalkdoc_1_0.DocBlocksQueryRequest{
    StartIndex: tea.Int32(0),
    EndIndex: tea.Int32(1),
    BlockType: tea.String("paragraph"),
    OperatorId: tea.String("union_id"),
  }
  tryErr := func()(_e error) {
    defer func() {
      if r := tea.Recover(recover()); r != nil {
        _e = r
      }
    }()
    _, _err = client.DocBlocksQueryWithOptions(tea.String("doc_id"), docBlocksQueryRequest, docBlocksQueryHeaders, &util.RuntimeOptions{})
    if _err != nil {
      return _err
    }

    return nil
  }()

  if tryErr != nil {
    var err = &tea.SDKError{}
    if _t, ok := tryErr.(*tea.SDKError); ok {
      err = _t
    } else {
      err.Message = tea.String(tryErr.Error())
    }
    if !tea.BoolValue(util.Empty(err.Code)) && !tea.BoolValue(util.Empty(err.Message)) {
      // err contains the code and message attributes, which help locate the issue
    }

  }
  return _err
}

func main() {
  err := _main(tea.StringSlice(os.Args[1:]))
  if err != nil {
    panic(err)
  }
}
```

Node.js

```text
'use strict';
// This file is auto-generated, don't edit it
const Util = require('@alicloud/tea-util');
const dingtalkdoc_1_0 = require('@alicloud/dingtalk/doc_1_0');
const OpenApi = require('@alicloud/openapi-client');
const Tea = require('@alicloud/tea-typescript');

class Client {

  /**
   * Initialize the account Client with a Token
   * @return Client
   * @throws Exception
   */
  static createClient() {
    let config = new OpenApi.Config({ });
    config.protocol = 'https';
    config.regionId = 'central';
    return new dingtalkdoc_1_0.default(config);
  }

  static async main(args) {
    let client = Client.createClient();
    let docBlocksQueryHeaders = new dingtalkdoc_1_0.DocBlocksQueryHeaders({ });
    docBlocksQueryHeaders.xAcsDingtalkAccessToken = '<your access token>';
    let docBlocksQueryRequest = new dingtalkdoc_1_0.DocBlocksQueryRequest({
      startIndex: 0,
      endIndex: 1,
      blockType: 'paragraph',
      operatorId: 'union_id',
    });
    try {
      await client.docBlocksQueryWithOptions('doc_id', docBlocksQueryRequest, docBlocksQueryHeaders, new Util.RuntimeOptions({ }));
    } catch (err) {
      if (!Util.default.empty(err.code) && !Util.default.empty(err.message)) {
        // err contains the code and message attributes, which help locate the issue
      }

    }    
  }

}

exports.Client = Client;
Client.main(process.argv.slice(2));
```

C#

```java
using Newtonsoft.Json;
using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

using Tea;
using Tea.Utils;

namespace AlibabaCloud.SDK.Sample
{
    public class Sample 
    {

        /// <term><b>Description:</b></term>
        /// <description>
        /// <para>Initialize the account Client with a Token</para>
        /// </description>
        /// 
        /// <returns>
        /// Client
        /// </returns>
        /// 
        /// <term><b>Exception:</b></term>
        /// Exception
        public static AlibabaCloud.SDK.Dingtalkdoc_1_0.Client CreateClient()
        {
            AlibabaCloud.OpenApiClient.Models.Config config = new AlibabaCloud.OpenApiClient.Models.Config();
            config.Protocol = "https";
            config.RegionId = "central";
            return new AlibabaCloud.SDK.Dingtalkdoc_1_0.Client(config);
        }

        public static void Main(string[] args)
        {
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Client client = CreateClient();
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocBlocksQueryHeaders docBlocksQueryHeaders = new AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocBlocksQueryHeaders();
            docBlocksQueryHeaders.XAcsDingtalkAccessToken = "<your access token>";
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocBlocksQueryRequest docBlocksQueryRequest = new AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocBlocksQueryRequest
            {
                StartIndex = 0,
                EndIndex = 1,
                BlockType = "paragraph",
                OperatorId = "union_id",
            };
            try
            {
                client.DocBlocksQueryWithOptions("doc_id", docBlocksQueryRequest, docBlocksQueryHeaders, new AlibabaCloud.TeaUtil.Models.RuntimeOptions());
            }
            catch (TeaException err)
            {
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err contains the code and message attributes, which help locate the issue
                }
            }
            catch (Exception _err)
            {
                TeaException err = new TeaException(new Dictionary<string, object>
                {
                    { "message", _err.Message }
                });
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err contains the code and message attributes, which help locate the issue
                }
            }
        }

    }
}
```

## Respons

### Isi respons

| Nama | Jenis | Penerangan |
| --- | --- | --- |
| success | boolean | Sama ada permintaan berjaya. |
| result | object | Hasil permintaan. |
| data | Array of Any | Setiap item ialah struktur elemen blok. |

### Contoh isi respons

```text
HTTP/1.1 200 OK
Content-Type:application/json

{
  "success": true,
  "result": {
    "data": [
      {
        "blockType": "paragraph",
        "paragraph": {
          "text": "Paragraph 1"
        },
        "id": "lc4si5p3n84zwqxxx",
        "index": 1,
      },
      {
        "blockType": "paragraph",
        "paragraph": {
          "text": "Paragraph 2"
        },
        "id": "lc4si5p3n84zwqyyy",
        "index": 1,
      }
    ]
  },
}
```

### Kod ralat

Jika ralat dikembalikan semasa anda memanggil API ini, rujuk dokumen [Kod ralat global](/ms/open/development/server-api-error-codes-1) untuk mencari penyelesaian berdasarkan mesej ralat.

| HttpCode | Kod ralat | Mesej ralat | Penerangan |
| --- | --- | --- | --- |
| 400 | paramError | %s | Ralat parameter. |
| 400 | invalidRequest.inputArgs.invalid | %s | Ralat parameter. Semak mesej ralat untuk maklumat terperinci. |
| 403 | forbidden.accessDenied | %s | Tiada kebenaran. |
| 403 | forbidden.acrossOrg | %s | Permintaan tidak sah. Semak sama ada dokumen yang ingin anda akses milik organisasi yang ditentukan oleh access token. |
| 404 | docNotExist | %s | Dokumen tidak wujud. |
| 404 | invalidRequest.resource.notFound | %s | Sumber yang diminta tidak dapat ditemui. |
| 500 | forbidden.operationIllegal | %s | Tindakan yang diminta tidak sah. |
| 500 | systemError | %s | Perkhidmatan sedang sibuk. Cuba Lagi kemudian. |
| 500 | unknownError | Unknown Error | Ralat tidak diketahui. |
| 503 | operationTimeout | %s | Permintaan tamat masa. |
