---
title: "Menanyakan dingId yang Telah Dimigrasi Berdasarkan dingId Asal"
description: "Selepas akaun standard dimigrasikan kepada Akaun Dedicated, panggil API ini untuk menanyakan dingId hasil migrasi berdasarkan dingId asal."
---

## Permintaan

### Maklumat asas

| Medan | Nilai |
| --- | --- |
| URL HTTP | https://api.dingtalk.io/v1.0/contact/orgAccount/getMigrationDingIdByDingIds |
| Kaedah HTTP | GET |
| Jenis aplikasi yang disokong | appType-Aplikasi dalaman　appType-Aplikasi perusahaan pihak ketiga |
| Kebenaran yang diperlukan | permission-Contact.OrgAccountMigration.Read-Kebenaran membaca data migrasi Akaun Syarikat Kenalan |

### Pengepala permintaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| x-acs-dingtalk-access-token | String | Ya | Kredensial akses untuk memanggil API ini. Dapatkan menggunakan cara berikut:   - Untuk aplikasi dalaman, panggil API [Dapatkan access token aplikasi dalaman](/ms/open/development/obtain-the-access-token-of-an-internal-app#). - Untuk aplikasi perusahaan pihak ketiga, panggil API [Dapatkan access token organisasi yang telah memberi kebenaran kepada aplikasi pihak ketiga](https://open.dingtalk.com/document/development/obtain-the-access-token-of-the-authorized-enterprise-1#). |

### Parameter kueri

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| dingId | String | Ya | dingId akaun standard asal. |

### Contoh permintaan

HTTP

```http
GET /v1.0/contact/orgAccount/getMigrationDingIdByDingIds?dingId=$:LWCP_v1:$yyyy HTTP/1.1
Host:api.dingtalk.io
x-acs-dingtalk-access-token:xxxx
Content-Type:application/json
```

Java

```java
// Fail ini dijana secara automatik, jangan edit. Terima kasih.
package com.aliyun.sample;

import com.aliyun.tea.*;
import com.aliyun.teautil.*;
import com.aliyun.teautil.models.*;
import com.aliyun.dingtalkcontact_1_0.*;
import com.aliyun.dingtalkcontact_1_0.models.*;
import com.aliyun.teaopenapi.*;
import com.aliyun.teaopenapi.models.*;

public class Sample {

    /**
     * Inisialisasi Client akaun dengan Token
     * @return Client
     * @throws Exception
     */
    public static com.aliyun.dingtalkcontact_1_0.Client createClient() throws Exception {
        Config config = new Config();
        config.protocol = "https";
        config.regionId = "central";
        return new com.aliyun.dingtalkcontact_1_0.Client(config);
    }

    public static void main(String[] args_) throws Exception {
        java.util.List<String> args = java.util.Arrays.asList(args_);
        com.aliyun.dingtalkcontact_1_0.Client client = Sample.createClient();
        GetMigrationDingIdByDingIdHeaders getMigrationDingIdByDingIdHeaders = new GetMigrationDingIdByDingIdHeaders();
        getMigrationDingIdByDingIdHeaders.xAcsDingtalkAccessToken = "<your access token>";
        GetMigrationDingIdByDingIdRequest getMigrationDingIdByDingIdRequest = new GetMigrationDingIdByDingIdRequest()
                .setDingId("$:LWCP_v1:$yyyy");
        try {
            client.getMigrationDingIdByDingIdWithOptions(getMigrationDingIdByDingIdRequest, getMigrationDingIdByDingIdHeaders, new RuntimeOptions());
        } catch (TeaException err) {
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err mengandungi sifat code dan message untuk membantu mengesan isu
            }

        } catch (Exception _err) {
            TeaException err = new TeaException(_err.getMessage(), _err);
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err mengandungi sifat code dan message untuk membantu mengesan isu
            }

        }        
    }
}
```

Python

```python
# -*- coding: utf-8 -*-
# Fail ini dijana secara automatik, jangan edit. Terima kasih.
import sys

from typing import List

from alibabacloud_dingtalk.contact_1_0.client import Client as dingtalkcontact_1_0Client
from alibabacloud_tea_openapi import models as open_api_models
from alibabacloud_dingtalk.contact_1_0 import models as dingtalkcontact__1__0_models
from alibabacloud_tea_util import models as util_models
from alibabacloud_tea_util.client import Client as UtilClient

class Sample:
    def __init__(self):
        pass

    @staticmethod
    def create_client() -> dingtalkcontact_1_0Client:
        """
        Inisialisasi Client akaun dengan Token
        @return: Client
        @throws Exception
        """
        config = open_api_models.Config()
        config.protocol = 'https'
        config.region_id = 'central'
        return dingtalkcontact_1_0Client(config)

    @staticmethod
    def main(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        get_migration_ding_id_by_ding_id_headers = dingtalkcontact__1__0_models.GetMigrationDingIdByDingIdHeaders()
        get_migration_ding_id_by_ding_id_headers.x_acs_dingtalk_access_token = '<your access token>'
        get_migration_ding_id_by_ding_id_request = dingtalkcontact__1__0_models.GetMigrationDingIdByDingIdRequest(
            ding_id='$:LWCP_v1:$yyyy'
        )
        try:
            client.get_migration_ding_id_by_ding_id_with_options(get_migration_ding_id_by_ding_id_request, get_migration_ding_id_by_ding_id_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err mengandungi sifat code dan message untuk membantu mengesan isu
                pass

    @staticmethod
    async def main_async(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        get_migration_ding_id_by_ding_id_headers = dingtalkcontact__1__0_models.GetMigrationDingIdByDingIdHeaders()
        get_migration_ding_id_by_ding_id_headers.x_acs_dingtalk_access_token = '<your access token>'
        get_migration_ding_id_by_ding_id_request = dingtalkcontact__1__0_models.GetMigrationDingIdByDingIdRequest(
            ding_id='$:LWCP_v1:$yyyy'
        )
        try:
            await client.get_migration_ding_id_by_ding_id_with_options_async(get_migration_ding_id_by_ding_id_request, get_migration_ding_id_by_ding_id_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err mengandungi sifat code dan message untuk membantu mengesan isu
                pass

if __name__ == '__main__':
    Sample.main(sys.argv[1:])
```

PHP

```java
<?php

// Fail ini dijana secara automatik, jangan edit. Terima kasih.
namespace AlibabaCloud\SDK\Sample;

use AlibabaCloud\SDK\Dingtalk\Vcontact_1_0\Dingtalk;
use \Exception;
use AlibabaCloud\Tea\Exception\TeaError;
use AlibabaCloud\Tea\Utils\Utils;

use Darabonba\OpenApi\Models\Config;
use AlibabaCloud\SDK\Dingtalk\Vcontact_1_0\Models\GetMigrationDingIdByDingIdHeaders;
use AlibabaCloud\SDK\Dingtalk\Vcontact_1_0\Models\GetMigrationDingIdByDingIdRequest;
use AlibabaCloud\Tea\Utils\Utils\RuntimeOptions;

class Sample {

    /**
     * Inisialisasi Client akaun dengan Token
     * @return Dingtalk Client
     */
    public static function createClient(){
        $config = new Config([]);
        $config->protocol = "https";
        $config->regionId = "central";
        return new Dingtalk($config);
    }

    /**
     * @param string[] $args
     * @return void
     */
    public static function main($args){
        $client = self::createClient();
        $getMigrationDingIdByDingIdHeaders = new GetMigrationDingIdByDingIdHeaders([]);
        $getMigrationDingIdByDingIdHeaders->xAcsDingtalkAccessToken = "<your access token>";
        $getMigrationDingIdByDingIdRequest = new GetMigrationDingIdByDingIdRequest([
            "dingId" => "\$:LWCP_v1:\$yyyy"
        ]);
        try {
            $client->getMigrationDingIdByDingIdWithOptions($getMigrationDingIdByDingIdRequest, $getMigrationDingIdByDingIdHeaders, new RuntimeOptions([]));
        }
        catch (Exception $err) {
            if (!($err instanceof TeaError)) {
                $err = new TeaError([], $err->getMessage(), $err->getCode(), $err);
            }
            if (!Utils::empty_($err->code) && !Utils::empty_($err->message)) {
                // err mengandungi sifat code dan message untuk membantu mengesan isu
            }
        }
    }
}
$path = __DIR__ . \DIRECTORY_SEPARATOR . '..' . \DIRECTORY_SEPARATOR . 'vendor' . \DIRECTORY_SEPARATOR . 'autoload.php';
if (file_exists($path)) {
    require_once $path;
}
Sample::main(array_slice($argv, 1));
```

Go

```go
// Fail ini dijana secara automatik, jangan edit. Terima kasih.
package main

import (
  "os"
  util  "github.com/alibabacloud-go/tea-utils/service"
  dingtalkcontact_1_0  "github.com/alibabacloud-go/dingtalk/contact_1_0"
  openapi  "github.com/alibabacloud-go/darabonba-openapi/client"
  "github.com/alibabacloud-go/tea/tea"
)

/**
 * Inisialisasi Client akaun dengan Token
 * @return Client
 * @throws Exception
 */
func CreateClient () (_result *dingtalkcontact_1_0.Client, _err error) {
  config := &openapi.Config{}
  config.Protocol = tea.String("https")
  config.RegionId = tea.String("central")
  _result = &dingtalkcontact_1_0.Client{}
  _result, _err = dingtalkcontact_1_0.NewClient(config)
  return _result, _err
}

func _main (args []*string) (_err error) {
  client, _err := CreateClient()
  if _err != nil {
    return _err
  }

  getMigrationDingIdByDingIdHeaders := &dingtalkcontact_1_0.GetMigrationDingIdByDingIdHeaders{}
  getMigrationDingIdByDingIdHeaders.XAcsDingtalkAccessToken = tea.String("<your access token>")
  getMigrationDingIdByDingIdRequest := &dingtalkcontact_1_0.GetMigrationDingIdByDingIdRequest{
    DingId: tea.String("$:LWCP_v1:$yyyy"),
  }
  tryErr := func()(_e error) {
    defer func() {
      if r := tea.Recover(recover()); r != nil {
        _e = r
      }
    }()
    _, _err = client.GetMigrationDingIdByDingIdWithOptions(getMigrationDingIdByDingIdRequest, getMigrationDingIdByDingIdHeaders, &util.RuntimeOptions{})
    if _err != nil {
      return _err
    }

    return nil
  }()

  if tryErr != nil {
    var err = &tea.SDKError{}
    if _t, ok := tryErr.(*tea.SDKError); ok {
      err = _t
    } else {
      err.Message = tea.String(tryErr.Error())
    }
    if !tea.BoolValue(util.Empty(err.Code)) && !tea.BoolValue(util.Empty(err.Message)) {
      // err mengandungi sifat code dan message untuk membantu mengesan isu
    }

  }
  return _err
}

func main() {
  err := _main(tea.StringSlice(os.Args[1:]))
  if err != nil {
    panic(err)
  }
}
```

Node.js

```python
// Fail ini dijana secara automatik, jangan edit
import Util, * as $Util from '@alicloud/tea-util';
import dingtalkcontact_1_0, * as $dingtalkcontact_1_0 from '@alicloud/dingtalk/contact_1_0';
import OpenApi, * as $OpenApi from '@alicloud/openapi-client';
import * as $tea from '@alicloud/tea-typescript';

export default class Client {

  /**
   * Inisialisasi Client akaun dengan Token
   * @return Client
   * @throws Exception
   */
  static createClient(): dingtalkcontact_1_0 {
    let config = new $OpenApi.Config({ });
    config.protocol = "https";
    config.regionId = "central";
    return new dingtalkcontact_1_0(config);
  }

  static async main(args: string[]): Promise<void> {
    let client = Client.createClient();
    let getMigrationDingIdByDingIdHeaders = new $dingtalkcontact_1_0.GetMigrationDingIdByDingIdHeaders({ });
    getMigrationDingIdByDingIdHeaders.xAcsDingtalkAccessToken = "<your access token>";
    let getMigrationDingIdByDingIdRequest = new $dingtalkcontact_1_0.GetMigrationDingIdByDingIdRequest({
      dingId: "$:LWCP_v1:$yyyy",
    });
    try {
      await client.getMigrationDingIdByDingIdWithOptions(getMigrationDingIdByDingIdRequest, getMigrationDingIdByDingIdHeaders, new $Util.RuntimeOptions({ }));
    } catch (err) {
      if (!Util.empty(err.code) && !Util.empty(err.message)) {
        // err mengandungi sifat code dan message untuk membantu mengesan isu
      }

    }    
  }

}

Client.main(process.argv.slice(2));
```

C#

```java
// Fail ini dijana secara automatik, jangan edit. Terima kasih.

using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

using Tea;
using Tea.Utils;

namespace AlibabaCloud.SDK.Sample
{
    public class Sample 
    {

        /**
         * Inisialisasi Client akaun dengan Token
         * @return Client
         * @throws Exception
         */
        public static AlibabaCloud.SDK.Dingtalkcontact_1_0.Client CreateClient()
        {
            AlibabaCloud.OpenApiClient.Models.Config config = new AlibabaCloud.OpenApiClient.Models.Config();
            config.Protocol = "https";
            config.RegionId = "central";
            return new AlibabaCloud.SDK.Dingtalkcontact_1_0.Client(config);
        }

        public static void Main(string[] args)
        {
            AlibabaCloud.SDK.Dingtalkcontact_1_0.Client client = CreateClient();
            AlibabaCloud.SDK.Dingtalkcontact_1_0.Models.GetMigrationDingIdByDingIdHeaders getMigrationDingIdByDingIdHeaders = new AlibabaCloud.SDK.Dingtalkcontact_1_0.Models.GetMigrationDingIdByDingIdHeaders();
            getMigrationDingIdByDingIdHeaders.XAcsDingtalkAccessToken = "<your access token>";
            AlibabaCloud.SDK.Dingtalkcontact_1_0.Models.GetMigrationDingIdByDingIdRequest getMigrationDingIdByDingIdRequest = new AlibabaCloud.SDK.Dingtalkcontact_1_0.Models.GetMigrationDingIdByDingIdRequest
            {
                DingId = "$:LWCP_v1:$yyyy",
            };
            try
            {
                client.GetMigrationDingIdByDingIdWithOptions(getMigrationDingIdByDingIdRequest, getMigrationDingIdByDingIdHeaders, new AlibabaCloud.TeaUtil.Models.RuntimeOptions());
            }
            catch (TeaException err)
            {
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err mengandungi sifat code dan message untuk membantu mengesan isu
                }
            }
            catch (Exception _err)
            {
                TeaException err = new TeaException(new Dictionary<string, object>
                {
                    { "message", _err.Message }
                });
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err mengandungi sifat code dan message untuk membantu mengesan isu
                }
            }
        }

    }
}
```

C

```cpp
// Fail ini dijana secara automatik, jangan edit. Terima kasih.

#include <alibabacloud/dingtalkcontact__1__0.hpp>
#include <alibabacloud/open_api.hpp>
#include <boost/any.hpp>
#include <darabonba/core.hpp>
#include <darabonba/util.hpp>
#include <iostream>
#include <map>

using namespace std;

Alibabacloud_Dingtalkcontact_1_0::Client createClient() {
  shared_ptr<Alibabacloud_OpenApi::Config> config = make_shared<Alibabacloud_OpenApi::Config>();
  config->protocol = make_shared<string>("https");
  config->regionId = make_shared<string>("central");
  return Alibabacloud_Dingtalkcontact_1_0::Client(config);
}

int main(int argc, char *args[]) {
  args;
  shared_ptr<Alibabacloud_Dingtalkcontact_1_0::Client> client = make_shared<Alibabacloud_Dingtalkcontact_1_0::Client>(createClient());
  shared_ptr<Alibabacloud_Dingtalkcontact_1_0::GetMigrationDingIdByDingIdHeaders> getMigrationDingIdByDingIdHeaders = make_shared<Alibabacloud_Dingtalkcontact_1_0::GetMigrationDingIdByDingIdHeaders>();
  getMigrationDingIdByDingIdHeaders->xAcsDingtalkAccessToken = make_shared<string>("<your access token>");
  shared_ptr<Alibabacloud_Dingtalkcontact_1_0::GetMigrationDingIdByDingIdRequest> getMigrationDingIdByDingIdRequest = make_shared<Alibabacloud_Dingtalkcontact_1_0::GetMigrationDingIdByDingIdRequest>(map<string, boost::any>({
    {"dingId", boost::any(string("$:LWCP_v1:$yyyy"))}
  }));
  try {
    client->getMigrationDingIdByDingIdWithOptions(getMigrationDingIdByDingIdRequest, getMigrationDingIdByDingIdHeaders, make_shared<Darabonba_Util::RuntimeOptions>(Darabonba_Util::RuntimeOptions()));
  }
  catch (std::exception &err) {
    if (!Darabonba_Util::Client::empty(err.code) && !Darabonba_Util::Client::empty(err.message)) {
      // err mengandungi sifat code dan message untuk membantu mengesan isu
    }
  }
}
```

## Respons

### Isi respons

| Nama | Jenis | Penerangan |
| --- | --- | --- |
| migrationDingIdList | Map | dingId Akaun Syarikat yang telah dimigrasi. |

### Contoh isi respons

```text
HTTP/1.1 200 OK
Content-Type:application/json

{
  "$:LWCP_v1" : "$zzz"
}
```

### Kod ralat

Jika ralat berlaku semasa memanggil API ini, cari penyelesaian dalam dokumen [Kod ralat global](/ms/open/development/server-api-error-codes-1) berdasarkan mesej ralat.

| HttpCode | Kod ralat | Mesej ralat | Penerangan |
| --- | --- | --- | --- |
| 400 | invalid.param | Invalid parameter | Parameter tidak sah |
| 400 | invalid.param | Invalid dingId | dingId tidak sah |
| 500 | system.busy | System busy | Sistem sibuk |
