---
title: "Tambah elemen dalaman ke akhir perenggan"
description: "Panggil API ini untuk menambah elemen dalaman ke akhir perenggan yang ditentukan, menyokong jenis seperti stiker, gambar dan pautan."
---

Panggil API ini untuk menambah elemen dalaman ke akhir perenggan yang ditentukan. Jenis elemen dalaman yang disokong termasuk Pelekat, Gambar dan Pautan.

## Penerangan panggilan API

Untuk butiran mengenai struktur data yang berkaitan dengan kandungan dokumen, lihat [Struktur data dokumen](/open/development/document-data-structure).

## Permintaan

### Maklumat asas

| Medan | Nilai |
| --- | --- |
| URL HTTP | `https://api.dingtalk.io/v1.0/doc/suites/documents/{docKey}/blocks/{blockId}/paragraph/appendElement` |
| Kaedah HTTP | POST |
| Jenis aplikasi yang disokong | appType-Aplikasi dalaman |
| Kebenaran yang diperlukan | permission-Storage.File.Write-Kebenaran tulis fail storan organisasi |

### Pengepala permintaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| x-acs-dingtalk-access-token | String | Ya | Kredensial akses untuk memanggil API ini. Dapatkan dengan memanggil API [Dapatkan access token aplikasi dalaman](/open/development/obtain-the-access-token-of-an-internal-app). |

### Parameter laluan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| docKey | String | Ya | ID Dokumen (`docKey` atau `dentryUuid` bagi dokumen tersebut). Selepas mencipta dokumen dengan memanggil API [Cipta dokumen Knowledge Base pasukan](/open/development/create-team-space-document), dapatkan parameter `docKey` daripada respons. Untuk mendapatkan `dentryUuid` daripada URL dokumen, ambil https://docs.dingtalk.io/i/nodes/Zxxxxa-id sebagai contoh: `dentryUuid` ialah `Zxxxxa-id`. |
| blockId | String | Ya | Pengecam unik bagi perenggan sasaran. Dapatkan parameter `blockId` dengan memanggil API [Tanya elemen blok](/open/development/api-docblocksquery). |

### Parameter pertanyaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| operatorId | String | Ya | unionId pengguna. Dapatkan nilai parameter `unionid` dengan memanggil API [Tanya butiran pengguna](/open/development/query-user-details). |

### Isi permintaan

| Nama | Jenis | Wajib | Penerangan |
| --- | --- | --- | --- |
| elementType | String | Ya | Jenis elemen dalaman. Untuk butiran, lihat jenis elemen dalaman dalam [Struktur data elemen blok](/open/development/block-data-structure). |
| properties | `Map<String, Any>` | Ya | Sifat yang diperlukan untuk mencipta elemen dalaman bagi `elementType` yang berkaitan. |
|  | Any | Tidak | Nilai sifat. |

### Contoh permintaan

HTTP

```http
POST /v1.0/doc/suites/documents/doc_id/blocks/block_id/paragraph/appendElement?operatorId=union_id HTTP/1.1
Host:api.dingtalk.io
x-acs-dingtalk-access-token:7dc9ec4xxx
Content-Type:application/json

{
  "elementType" : "sticker",
  "properties" : {
    "key" : "{}"
  }
}
```

Java

```java
package com.aliyun.sample;

import com.aliyun.tea.*;

public class Sample {

    /**
     * <b>description</b> :
     * <p>Initialize the account Client with a token</p>
     * @return Client
     * 
     * @throws Exception
     */
    public static com.aliyun.dingtalkdoc_1_0.Client createClient() throws Exception {
        com.aliyun.teaopenapi.models.Config config = new com.aliyun.teaopenapi.models.Config();
        config.protocol = "https";
        config.regionId = "central";
        return new com.aliyun.dingtalkdoc_1_0.Client(config);
    }

    public static void main(String[] args_) throws Exception {
        
        com.aliyun.dingtalkdoc_1_0.Client client = Sample.createClient();
        com.aliyun.dingtalkdoc_1_0.models.DocAppendParagraphHeaders docAppendParagraphHeaders = new com.aliyun.dingtalkdoc_1_0.models.DocAppendParagraphHeaders();
        docAppendParagraphHeaders.xAcsDingtalkAccessToken = "<your access token>";
        java.util.Map<String, java.util.Map<String, Object>> properties = TeaConverter.buildMap(
            new TeaPair("key", new java.util.HashMap<>())
        );
        com.aliyun.dingtalkdoc_1_0.models.DocAppendParagraphRequest docAppendParagraphRequest = new com.aliyun.dingtalkdoc_1_0.models.DocAppendParagraphRequest()
                .setOperatorId("union_id")
                .setElementType("sticker")
                .setProperties(properties);
        try {
            client.docAppendParagraphWithOptions("doc_id", "block_id", docAppendParagraphRequest, docAppendParagraphHeaders, new com.aliyun.teautil.models.RuntimeOptions());
        } catch (TeaException err) {
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err contains the code and message attributes, which can help developers locate the issue
            }

        } catch (Exception _err) {
            TeaException err = new TeaException(_err.getMessage(), _err);
            if (!com.aliyun.teautil.Common.empty(err.code) && !com.aliyun.teautil.Common.empty(err.message)) {
                // err contains the code and message attributes, which can help developers locate the issue
            }

        }        
    }
}
```

Python

```python
# -*- coding: utf-8 -*-
# This file is auto-generated, don't edit it. Thanks.
import os
import sys
import json

from typing import List

from alibabacloud_dingtalk.doc_1_0.client import Client as dingtalkdoc_1_0Client
from alibabacloud_tea_openapi import models as open_api_models
from alibabacloud_dingtalk.doc_1_0 import models as dingtalkdoc__1__0_models
from alibabacloud_tea_util import models as util_models
from alibabacloud_tea_util.client import Client as UtilClient

class Sample:
    def __init__(self):
        pass

    @staticmethod
    def create_client() -> dingtalkdoc_1_0Client:
        """
        Initialize the account Client with a token
        @return: Client
        @throws Exception
        """
        config = open_api_models.Config()
        config.protocol = 'https'
        config.region_id = 'central'
        return dingtalkdoc_1_0Client(config)

    @staticmethod
    def main(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        doc_append_paragraph_headers = dingtalkdoc__1__0_models.DocAppendParagraphHeaders()
        doc_append_paragraph_headers.x_acs_dingtalk_access_token = '<your access token>'
        properties = {
            'key': {}
        }
        doc_append_paragraph_request = dingtalkdoc__1__0_models.DocAppendParagraphRequest(
            operator_id='union_id',
            element_type='sticker',
            properties=properties
        )
        try:
            client.doc_append_paragraph_with_options('doc_id', 'block_id', doc_append_paragraph_request, doc_append_paragraph_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err contains the code and message attributes, which can help developers locate the issue
                pass

    @staticmethod
    async def main_async(
        args: List[str],
    ) -> None:
        client = Sample.create_client()
        doc_append_paragraph_headers = dingtalkdoc__1__0_models.DocAppendParagraphHeaders()
        doc_append_paragraph_headers.x_acs_dingtalk_access_token = '<your access token>'
        properties = {
            'key': {}
        }
        doc_append_paragraph_request = dingtalkdoc__1__0_models.DocAppendParagraphRequest(
            operator_id='union_id',
            element_type='sticker',
            properties=properties
        )
        try:
            await client.doc_append_paragraph_with_options_async('doc_id', 'block_id', doc_append_paragraph_request, doc_append_paragraph_headers, util_models.RuntimeOptions())
        except Exception as err:
            if not UtilClient.empty(err.code) and not UtilClient.empty(err.message):
                # err contains the code and message attributes, which can help developers locate the issue
                pass

if __name__ == '__main__':
    Sample.main(sys.argv[1:])
```

PHP

```java
<?php

// This file is auto-generated, don't edit it. Thanks.
namespace AlibabaCloud\SDK\Sample;

use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Dingtalk;
use \Exception;
use AlibabaCloud\Tea\Exception\TeaError;
use AlibabaCloud\Tea\Utils\Utils;

use Darabonba\OpenApi\Models\Config;
use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Models\DocAppendParagraphHeaders;
use AlibabaCloud\SDK\Dingtalk\Vdoc_1_0\Models\DocAppendParagraphRequest;
use AlibabaCloud\Tea\Utils\Utils\RuntimeOptions;

class Sample {

    /**
     * Initialize the account Client with a token
     * @return Dingtalk Client
     */
    public static function createClient(){
        $config = new Config([]);
        $config->protocol = "https";
        $config->regionId = "central";
        return new Dingtalk($config);
    }

    /**
     * @param string[] $args
     * @return void
     */
    public static function main($args){
        $client = self::createClient();
        $docAppendParagraphHeaders = new DocAppendParagraphHeaders([]);
        $docAppendParagraphHeaders->xAcsDingtalkAccessToken = "<your access token>";
        $properties = [
            "key" => []
        ];
        $docAppendParagraphRequest = new DocAppendParagraphRequest([
            "operatorId" => "union_id",
            "elementType" => "sticker",
            "properties" => $properties
        ]);
        try {
            $client->docAppendParagraphWithOptions("doc_id", "block_id", $docAppendParagraphRequest, $docAppendParagraphHeaders, new RuntimeOptions([]));
        }
        catch (Exception $err) {
            if (!($err instanceof TeaError)) {
                $err = new TeaError([], $err->getMessage(), $err->getCode(), $err);
            }
            if (!Utils::empty_($err->code) && !Utils::empty_($err->message)) {
                // err contains the code and message attributes, which can help developers locate the issue
            }
        }
    }
}
$path = __DIR__ . \DIRECTORY_SEPARATOR . '..' . \DIRECTORY_SEPARATOR . 'vendor' . \DIRECTORY_SEPARATOR . 'autoload.php';
if (file_exists($path)) {
    require_once $path;
}
Sample::main(array_slice($argv, 1));
```

Go

```go
package main

import (
  "encoding/json"
  "strings"
  "fmt"
  "os"
  util  "github.com/alibabacloud-go/tea-utils/v2/service"
  dingtalkdoc_1_0  "github.com/alibabacloud-go/dingtalk/doc_1_0"
  openapi  "github.com/alibabacloud-go/darabonba-openapi/v2/client"
  "github.com/alibabacloud-go/tea/tea"
)

// Description:
// 
// Initialize the account Client with a token
// 
// @return Client
// 
// @throws Exception
func CreateClient () (_result *dingtalkdoc_1_0.Client, _err error) {
  config := &openapi.Config{}
  config.Protocol = tea.String("https")
  config.RegionId = tea.String("central")
  _result = &dingtalkdoc_1_0.Client{}
  _result, _err = dingtalkdoc_1_0.NewClient(config)
  return _result, _err
}

func _main (args []*string) (_err error) {
  client, _err := CreateClient()
  if _err != nil {
    return _err
  }

  docAppendParagraphHeaders := &dingtalkdoc_1_0.DocAppendParagraphHeaders{}
  docAppendParagraphHeaders.XAcsDingtalkAccessToken = tea.String("<your access token>")
  properties := map[string]map[string]interface{}{
    "key": map[string]interface{}{},
  }
  docAppendParagraphRequest := &dingtalkdoc_1_0.DocAppendParagraphRequest{
    OperatorId: tea.String("union_id"),
    ElementType: tea.String("sticker"),
    Properties: properties,
  }
  tryErr := func()(_e error) {
    defer func() {
      if r := tea.Recover(recover()); r != nil {
        _e = r
      }
    }()
    _, _err = client.DocAppendParagraphWithOptions(tea.String("doc_id"), tea.String("block_id"), docAppendParagraphRequest, docAppendParagraphHeaders, &util.RuntimeOptions{})
    if _err != nil {
      return _err
    }

    return nil
  }()

  if tryErr != nil {
    var err = &tea.SDKError{}
    if _t, ok := tryErr.(*tea.SDKError); ok {
      err = _t
    } else {
      err.Message = tea.String(tryErr.Error())
    }
    if !tea.BoolValue(util.Empty(err.Code)) && !tea.BoolValue(util.Empty(err.Message)) {
      // err contains the code and message attributes, which can help developers locate the issue
    }

  }
  return _err
}

func main() {
  err := _main(tea.StringSlice(os.Args[1:]))
  if err != nil {
    panic(err)
  }
}
```

Node.js

```text
'use strict';
// This file is auto-generated, don't edit it
const Util = require('@alicloud/tea-util');
const dingtalkdoc_1_0 = require('@alicloud/dingtalk/doc_1_0');
const OpenApi = require('@alicloud/openapi-client');
const Tea = require('@alicloud/tea-typescript');

class Client {

  /**
   * Initialize the account Client with a token
   * @return Client
   * @throws Exception
   */
  static createClient() {
    let config = new OpenApi.Config({ });
    config.protocol = 'https';
    config.regionId = 'central';
    return new dingtalkdoc_1_0.default(config);
  }

  static async main(args) {
    let client = Client.createClient();
    let docAppendParagraphHeaders = new dingtalkdoc_1_0.DocAppendParagraphHeaders({ });
    docAppendParagraphHeaders.xAcsDingtalkAccessToken = '<your access token>';
    let properties = {
      key: { },
    };
    let docAppendParagraphRequest = new dingtalkdoc_1_0.DocAppendParagraphRequest({
      operatorId: 'union_id',
      elementType: 'sticker',
      properties: properties,
    });
    try {
      await client.docAppendParagraphWithOptions('doc_id', 'block_id', docAppendParagraphRequest, docAppendParagraphHeaders, new Util.RuntimeOptions({ }));
    } catch (err) {
      if (!Util.default.empty(err.code) && !Util.default.empty(err.message)) {
        // err contains the code and message attributes, which can help developers locate the issue
      }

    }    
  }

}

exports.Client = Client;
Client.main(process.argv.slice(2));
```

C#

```java
using Newtonsoft.Json;
using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Threading.Tasks;

using Tea;
using Tea.Utils;

namespace AlibabaCloud.SDK.Sample
{
    public class Sample 
    {

        /// <term><b>Description:</b></term>
        /// <description>
        /// <para>Initialize the account Client with a token</para>
        /// </description>
        /// 
        /// <returns>
        /// Client
        /// </returns>
        /// 
        /// <term><b>Exception:</b></term>
        /// Exception
        public static AlibabaCloud.SDK.Dingtalkdoc_1_0.Client CreateClient()
        {
            AlibabaCloud.OpenApiClient.Models.Config config = new AlibabaCloud.OpenApiClient.Models.Config();
            config.Protocol = "https";
            config.RegionId = "central";
            return new AlibabaCloud.SDK.Dingtalkdoc_1_0.Client(config);
        }

        public static void Main(string[] args)
        {
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Client client = CreateClient();
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocAppendParagraphHeaders docAppendParagraphHeaders = new AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocAppendParagraphHeaders();
            docAppendParagraphHeaders.XAcsDingtalkAccessToken = "<your access token>";
            Dictionary<string, Dictionary<string, object>> properties = new Dictionary<string, Dictionary<string, object>>
            {
                {"key", new Dictionary<string, object>(){}},
            };
            AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocAppendParagraphRequest docAppendParagraphRequest = new AlibabaCloud.SDK.Dingtalkdoc_1_0.Models.DocAppendParagraphRequest
            {
                OperatorId = "union_id",
                ElementType = "sticker",
                Properties = properties,
            };
            try
            {
                client.DocAppendParagraphWithOptions("doc_id", "block_id", docAppendParagraphRequest, docAppendParagraphHeaders, new AlibabaCloud.TeaUtil.Models.RuntimeOptions());
            }
            catch (TeaException err)
            {
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err contains the code and message attributes, which can help developers locate the issue
                }
            }
            catch (Exception _err)
            {
                TeaException err = new TeaException(new Dictionary<string, object>
                {
                    { "message", _err.Message }
                });
                if (!AlibabaCloud.TeaUtil.Common.Empty(err.Code) && !AlibabaCloud.TeaUtil.Common.Empty(err.Message))
                {
                    // err contains the code and message attributes, which can help developers locate the issue
                }
            }
        }

    }
}
```

## Respons

### Isi respons

| Nama | Jenis | Penerangan |
| --- | --- | --- |
| success | Boolean | Sama ada permintaan berjaya. |
| result | Object | Hasil permintaan. |
| data | `Map<String, Any>` | Objek elemen dalaman yang dimasukkan. |
|  | Any | Hasil yang dikembalikan. |

### Contoh isi respons

```text
HTTP/1.1 200 OK
Content-Type:application/json

{
  "result" : {
    "data" : {
      "elementId" : "xxxxxx",
      "sticker" : {
        "code" : "偷笑"  // Senyum sinis
      },
      "index" : 1,
      "elementType" : "sticker",
      "parentId" : "xxxxxx"
    }
  },
  "success" : true
}
```

### Kod ralat

Jika berlaku ralat semasa memanggil API ini, cari penyelesaian dalam dokumentasi [Kod ralat global](/open/development/server-api-error-codes-1) berdasarkan mesej ralat.

| HttpCode | Kod ralat | Mesej ralat | Penerangan |
| --- | --- | --- | --- |
| 400 | paramError | %s | Ralat parameter. |
| 400 | invalidRequest.inputArgs.invalid | %s | Ralat parameter. Semak mesej ralat untuk butiran. |
| 403 | forbidden.accessDenied | %s | Tiada kebenaran. |
| 403 | forbidden.acrossOrg | %s | Permintaan tidak sah. Sahkan bahawa dokumen yang diakses adalah milik organisasi yang ditentukan oleh access token. |
| 404 | docNotExist | %s | Dokumen tidak wujud. |
| 404 | invalidRequest.resource.notFound | %s | Sumber yang diakses tidak dapat ditemui. |
| 500 | forbidden.operationIllegal | %s | Operasi yang diminta tidak sah. |
| 500 | systemError | %s | Perkhidmatan sedang sibuk. Cuba Lagi kemudian. |
| 500 | unknownError | Unknown Error | Ralat tidak diketahui. |
| 503 | operationTimeout | %s | Permintaan tamat masa. |
