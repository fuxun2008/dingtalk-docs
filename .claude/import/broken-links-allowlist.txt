# Broken-links allowlist — AI Table 导入
#
# 用途：mint broken-links 检测到这些 URL/正则模式时，视为"已知失效但允许保留"，
# 不作为构建阻断错误。
#
# 格式：
#   - 每行一个 URL 前缀或正则（# 起始为注释，空行忽略）
#   - 前缀匹配优先；行内含 `^` 或 `[` 等正则字符时作为正则处理
#
# 添加准则：
#   1. 仅记录"已知失效且无替代域名"的外链；任何可修复的链接（如 .com → .io 替换）
#      不要放进白名单，先修。
#   2. 添加每条时附上简短理由注释，便于将来 review。
#   3. 内部链接（站内 / zh/aitable/ 内）一律不进白名单 —— 内链失败必须修。
#
# 当前白名单：
# --------------------------------------------------------------------

# === mint broken-links 工具误报（含 @ 字符的图片） ===
# 含 @1x 标记的截图文件名（51 处）。文件存在于仓库，mint dev 200 OK。
# 通配模式：路径以 /zh/aitable/ 开头且文件名含 @1x，统一豁免。
^/zh/aitable/.*@1x.*\.png$

# === mint broken-links 工具误报（URL 含 %20 编码空格） ===
# 175 个磁盘文件名含字面空格（DingTalk 录屏导出格式），mdx 中已用 %20 URL-encode。
# Mintlify 浏览器渲染（mint dev）会 URL-decode 后正确返回文件；
# 但 mint broken-links 静态检查器直接比对 URL，把 %20 当字面量找不到 → 误报。
# 共 ~160 条 URL 命中（128 unique），用通配豁免。
^/zh/aitable/.*%20.*\.(png|gif|jpe?g|mp4|mov)$

# === mint broken-links 工具误报（含中文/混合字符的图片） ===
# 以下文件已存在仓库，mint dev 实际服务 200 OK，检查器误报。
# 显式列出以便将来 review。
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S1-系统管理入口_YxRdWe9_pL.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S2-系统集成-EngineCode_aDym7gE7gm.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/获取AppCode1_MxAND39mlU.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S4-应用配置-应用编码_AolgC-YSym.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S5-设计表单按钮_Hpa3yT70Bx.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S6-后端代码Tab_vSDKaeKQ3o.png
/zh/aitable/data-connectors/data-connectors-overview/huayun/images/S7-保存成功_7KmQz7-33u.png
/zh/aitable/formulas/function-practices/mom-growth-rate/images/图2_5H2zB0Omhw.png
/zh/aitable/formulas/function-practices/mom-growth-rate/images/图3_ckpPYcA6_-.png

# === 源端缺图（wolai 导出丢失，已编档） ===
# 详见 .claude/import/aitable-src-missing.md（85 unique 引用，broken-links 报告中
# 显示 49 条：38 个 .png.mark.png + 7 个 URL-encoded + 4 个截断的无扩展名路径）。
# 不在此处逐条列出，跑 mint broken-links 时直接交叉比对 aitable-src-missing.md。
